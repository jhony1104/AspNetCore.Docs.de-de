---
title: Versionierung von GrpC-Diensten
author: jamesnk
description: Erfahren Sie, wie Sie GrpC-Dienste in Version
monikerRange: '>= aspnetcore-3.0'
ms.author: jamesnk
ms.date: 01/09/2020
uid: grpc/versioning
ms.openlocfilehash: 61dd5dfca4064af3ae58ac288a1ee636450ff56b
ms.sourcegitcommit: 79850db9e79b1705b89f466c6f2c961ff15485de
ms.translationtype: HT
ms.contentlocale: de-DE
ms.lasthandoff: 01/07/2020
ms.locfileid: "75694193"
---
# <a name="versioning-grpc-services"></a><span data-ttu-id="bb8f0-103">Versionierung von GrpC-Diensten</span><span class="sxs-lookup"><span data-stu-id="bb8f0-103">Versioning gRPC services</span></span>

<span data-ttu-id="bb8f0-104">Von [James Newton-King](https://twitter.com/jamesnk)</span><span class="sxs-lookup"><span data-stu-id="bb8f0-104">By [James Newton-King](https://twitter.com/jamesnk)</span></span>

<span data-ttu-id="bb8f0-105">Neue Features, die einer app hinzugefügt werden, können für Clients bereitgestellte GrpC-Dienste erfordern, was manchmal auf unerwartete und unterbrechende Weise der Fall ist.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-105">New features added to an app can require gRPC services provided to clients to change, sometimes in unexpected and breaking ways.</span></span> <span data-ttu-id="bb8f0-106">Wenn sich die GrpC-Dienste ändern:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-106">When gRPC services change:</span></span>

* <span data-ttu-id="bb8f0-107">Beachten Sie, welche Auswirkungen Änderungen auf Clients haben.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-107">Consideration should be given on how changes impact clients.</span></span>
* <span data-ttu-id="bb8f0-108">Eine Versions Verwaltungs Strategie zur Unterstützung von Änderungen sollte implementiert werden.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-108">A versioning strategy to support changes should be implemented.</span></span>

## <a name="backwards-compatibility"></a><span data-ttu-id="bb8f0-109">Abwärtskompatibilität</span><span class="sxs-lookup"><span data-stu-id="bb8f0-109">Backwards compatibility</span></span>

<span data-ttu-id="bb8f0-110">Das GrpC-Protokoll dient zur Unterstützung von Diensten, die sich im Laufe der Zeit ändern.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-110">The gRPC protocol is designed to support services that change over time.</span></span> <span data-ttu-id="bb8f0-111">Allgemeine Ergänzungen zu den GrpC-Diensten und-Methoden sind nicht unterbrechend.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-111">Generally additions to gRPC services and methods are non-breaking.</span></span> <span data-ttu-id="bb8f0-112">Nicht unterbrechend bedeutet, dass vorhandene Clients weiterhin funktionieren.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-112">Non-breaking means existing clients continue to work.</span></span> <span data-ttu-id="bb8f0-113">Das ändern oder Löschen von GrpC-Diensten sind wichtige Änderungen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-113">Changing or deleting gRPC services are breaking changes.</span></span> <span data-ttu-id="bb8f0-114">Wichtige Änderungen bedeuten, dass vorhandene Clients fehlschlagen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-114">Breaking changes mean existing clients fail.</span></span>

<span data-ttu-id="bb8f0-115">Das vornehmen von nicht wichtigen Änderungen an einem Dienst hat eine Reihe von Vorteilen:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-115">Making non-breaking changes to a service has a number of benefits:</span></span>

- <span data-ttu-id="bb8f0-116">Vorhandene Clients werden weiterhin ausgeführt.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-116">Existing clients continue to run.</span></span>
- <span data-ttu-id="bb8f0-117">Vermeidet die Arbeit bei der Benachrichtigung von Clients über wichtige Änderungen und deren Aktualisierung.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-117">Avoids work involved with notifying clients of breaking changes, and updating them.</span></span>
- <span data-ttu-id="bb8f0-118">Es muss nur eine Version des Dienstanbieter dokumentiert und gewartet werden.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-118">Only one version of the service needs to be documented and maintained.</span></span>

<span data-ttu-id="bb8f0-119">Dieser Inhalt konzentriert sich darauf, ob Änderungen **unter dem GrpC-Protokoll und dem .net-Binär Kompatibilitäts Grad unterbrechen**.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-119">This content focuses on whether changes are **breaking at a gRPC protocol and .NET binary compatibility level**.</span></span> <span data-ttu-id="bb8f0-120">Wenn Sie Änderungen vornehmen, sollten Sie überprüfen, ob ältere Clients logisch weiter arbeiten können.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-120">When making changes, consider whether older clients can logically continue working.</span></span> <span data-ttu-id="bb8f0-121">Beispiel: Hinzufügen eines neuen Felds zu einer Anforderungs Nachricht:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-121">For example, adding a new field to a request message:</span></span>

* <span data-ttu-id="bb8f0-122">Ist kein Protokoll Breaking Change.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-122">Is not a protocol breaking change.</span></span>
* <span data-ttu-id="bb8f0-123">Wenn Sie einen Fehlerstatus auf dem Server zurückgeben, wenn das neue Feld nicht festgelegt ist, ist es ein Breaking Change für alte Clients.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-123">Returning an error status on the server if the new field is not set makes it a breaking change for old clients.</span></span>

### <a name="non-breaking-changes"></a><span data-ttu-id="bb8f0-124">Geringfügige Änderungen</span><span class="sxs-lookup"><span data-stu-id="bb8f0-124">Non-breaking changes</span></span>

<span data-ttu-id="bb8f0-125">Diese Änderungen sind nicht unterbrechend auf der GrpC-Protokollebene und der .net-Binär Ebene.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-125">These changes are non-breaking at a gRPC protocol level, and .NET binary level.</span></span>

- <span data-ttu-id="bb8f0-126">**Hinzufügen eines neuen Dienstanbieter**</span><span class="sxs-lookup"><span data-stu-id="bb8f0-126">**Adding a new service**</span></span>
- <span data-ttu-id="bb8f0-127">**Hinzufügen einer neuen Methode zu einem Dienst**</span><span class="sxs-lookup"><span data-stu-id="bb8f0-127">**Adding a new method to a service**</span></span>
- <span data-ttu-id="bb8f0-128">**Hinzufügen eines Felds zu einer Anforderungs Nachricht** : Felder, die einer Anforderungs Nachricht hinzugefügt werden, werden auf dem Server mit dem [Standardwert](https://developers.google.com/protocol-buffers/docs/proto3#default) deserialisiert, wenn Sie nicht festgelegt sind.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-128">**Adding a field to a request message** - Fields added to a request message are deserialized with the [default value](https://developers.google.com/protocol-buffers/docs/proto3#default) on the server when not set.</span></span> <span data-ttu-id="bb8f0-129">Damit der Dienst nicht unterbrochen wird, muss der Dienst erfolgreich ausgeführt werden, wenn er nicht von älteren Clients festgelegt wird.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-129">To be non-breaking the service will need to succeed when it is not set by older clients.</span></span>
- <span data-ttu-id="bb8f0-130">**Hinzufügen eines Felds zu einer Antwortnachricht** : Felder, die einer Antwortnachricht hinzugefügt werden, werden in die Auflistung [unbekannter Felder](https://developers.google.com/protocol-buffers/docs/proto3#unknowns) der Nachricht auf dem Client deserialisiert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-130">**Adding a field to a response message** - Fields added to a response message are deserialized into the message's [unknown fields](https://developers.google.com/protocol-buffers/docs/proto3#unknowns) collection on the client.</span></span>
- <span data-ttu-id="bb8f0-131">Das **Hinzufügen eines Werts zu** enumerationsenumerationen wird als numerischer Wert serialisiert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-131">**Adding a value to an enum** - Enums are serialized as a numeric value.</span></span> <span data-ttu-id="bb8f0-132">Neue Enumerationswerte werden auf dem Client auf den-Enumerationswert ohne Enumerationsnamen deserialisiert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-132">New enum values are deserialized on the client to the enum value without an enum name.</span></span> <span data-ttu-id="bb8f0-133">Ältere Clients müssen ordnungsgemäß ausgeführt werden, wenn Sie den Wert erhalten und nicht ausstehen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-133">To be non-breaking older clients will need to run correctly when they receive and unexcepted value.</span></span>

### <a name="binary-breaking-changes"></a><span data-ttu-id="bb8f0-134">Binäre wichtige Änderungen</span><span class="sxs-lookup"><span data-stu-id="bb8f0-134">Binary breaking changes</span></span>

<span data-ttu-id="bb8f0-135">Die folgenden Änderungen sind auf der GrpC-Protokollebene nicht unterbrechend, aber der Client muss aktualisiert werden, wenn er auf den neuesten *. proto* -Vertrag oder die Client .NET-Assembly aktualisiert wird.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-135">The following changes are non-breaking at a gRPC protocol level, but the client needs to be updated if it upgrades to the latest *.proto* contract or client .NET assembly.</span></span> <span data-ttu-id="bb8f0-136">Die binäre Kompatibilität ist wichtig, wenn Sie beabsichtigen, eine GrpC-Bibliothek in nuget zu veröffentlichen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-136">Binary compatibility is important if you plan to publish a gRPC library to NuGet.</span></span>

- <span data-ttu-id="bb8f0-137">Das **Entfernen von Feldwerten** aus einem entfernten Feld wird in die [unbekannten Felder](https://developers.google.com/protocol-buffers/docs/proto3#unknowns)der Nachricht deserialisiert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-137">**Removing a field** - Values from a removed field are deserialized to a message's [unknown fields](https://developers.google.com/protocol-buffers/docs/proto3#unknowns).</span></span> <span data-ttu-id="bb8f0-138">Dabei handelt es sich nicht um ein GrpC-Protokoll Breaking Change, aber der Client muss aktualisiert werden, wenn ein Upgrade auf den letzten Vertrag durchführt.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-138">This isn't a gRPC protocol breaking change, but the client needs to be updated if it upgrades to the latest contract.</span></span> <span data-ttu-id="bb8f0-139">Es ist wichtig, dass eine entfernte Feldnummer in Zukunft nicht versehentlich wieder verwendet wird.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-139">It is important that a removed field number isn't accidentally reused in the future.</span></span> <span data-ttu-id="bb8f0-140">Eine Möglichkeit, dies sicherzustellen, ist das Angeben von gelöschten Feldnummern und Namen in der Nachricht mithilfe des [`reserved`](https://developers.google.com/protocol-buffers/docs/proto3#reserved) -Schlüssel Worts von protobuf.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-140">One way to make sure this doesn't happen is to specify deleted field numbers and names on the message using Protobuf's [`reserved`](https://developers.google.com/protocol-buffers/docs/proto3#reserved) keyword.</span></span>
- <span data-ttu-id="bb8f0-141">Das **Umbenennen von feldfeldnamen** wird nur in generiertem Code verwendet.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-141">**Renaming a field** - Field names are only used in generated code.</span></span> <span data-ttu-id="bb8f0-142">Die Feldzahl wird verwendet, um Felder im Netzwerk zu identifizieren.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-142">The field number is used to identify fields on the network.</span></span> <span data-ttu-id="bb8f0-143">Wenn ein Upgrade auf den letzten Vertrag durchführt, muss der Client aktualisiert werden.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-143">The client will need to be updated if it upgrades to the latest contract.</span></span>
- <span data-ttu-id="bb8f0-144">Das **Umbenennen von Nachrichten** Namen wird nicht im Netzwerk gesendet, sodass dies kein GrpC-Protokoll Breaking Change ist. der Client muss jedoch aktualisiert werden, wenn ein Upgrade auf den letzten Vertrag durchführt.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-144">**Renaming a message** - Message names are not sent on the network so this isn't a gRPC protocol breaking change, but the client will need to be updated if it upgrades to the latest contract.</span></span>
- <span data-ttu-id="bb8f0-145">Wenn Sie **csharp_namespace** ändernde `csharp_namespace` ändern, ändert sich der Namespace generierter .NET-Typen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-145">**Changing csharp_namespace** - Changing `csharp_namespace` will change the namespace of generated .NET types.</span></span> <span data-ttu-id="bb8f0-146">Dabei handelt es sich nicht um ein GrpC-Protokoll Breaking Change, aber der Client muss aktualisiert werden, wenn ein Upgrade auf den letzten Vertrag durchführt.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-146">This isn't a gRPC protocol breaking change, but the client needs to be updated if it upgrades to the latest contract.</span></span>

### <a name="breaking-changes"></a><span data-ttu-id="bb8f0-147">Breaking Changes</span><span class="sxs-lookup"><span data-stu-id="bb8f0-147">Breaking changes</span></span>

<span data-ttu-id="bb8f0-148">Dabei handelt es sich um Protokoll-und binäre wichtige Änderungen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-148">These are protocol and binary breaking changes.</span></span>

- <span data-ttu-id="bb8f0-149">**Ändern eines Feld Datentyps** : das Ändern des Datentyps eines Felds in einen nicht [kompatiblen Typ](https://developers.google.com/protocol-buffers/docs/proto3#updating) führt beim Deserialisieren der Nachricht zu Fehlern.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-149">**Changing a field data type** - Changing a field's data type to an [incompatible type](https://developers.google.com/protocol-buffers/docs/proto3#updating) will cause errors when deserializing the message.</span></span> <span data-ttu-id="bb8f0-150">Auch wenn der neue Datentyp kompatibel ist, ist es wahrscheinlich, dass der Client aktualisiert werden muss, um den neuen Typ zu unterstützen, wenn er auf den letzten Vertrag aktualisiert wird.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-150">Even if the new data type is compatible, it is likely the client will need to be updated to support the new type if it upgrades to the latest contract.</span></span>
- <span data-ttu-id="bb8f0-151">**Ändern einer Feldzahl** : die Feldzahl wird verwendet, um Felder im Netzwerk zu identifizieren.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-151">**Changing a field number** - The field number is used to identify fields on the network.</span></span>
- <span data-ttu-id="bb8f0-152">**Umbenennen eines Pakets, eines dienstangangs oder einer Methode** : GrpC verwendet den Paketnamen, den Dienstnamen und den Methodennamen, um die URL zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-152">**Renaming a package, service or method** - gRPC uses the package name, service name and method name to build the URL.</span></span> <span data-ttu-id="bb8f0-153">Der Client erhält einen *nicht implementierten* Status vom Server.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-153">The client gets an *UNIMPLEMENTED* status from the server.</span></span>
- <span data-ttu-id="bb8f0-154">**Entfernen eines Dienstes oder einer Methode** : der Client erhält einen *nicht implementierten* Status vom Server, wenn die entfernte Methode aufgerufen wird.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-154">**Removing a service or method** - The client gets an *UNIMPLEMENTED* status from the server when calling the removed method.</span></span>

## <a name="version-number-services"></a><span data-ttu-id="bb8f0-155">Versionsnummern Dienste</span><span class="sxs-lookup"><span data-stu-id="bb8f0-155">Version number services</span></span>

<span data-ttu-id="bb8f0-156">Dienste sollten eine Abwärtskompatibilität mit alten Clients anstreben.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-156">Services should strive to remain backwards compatible with old clients.</span></span> <span data-ttu-id="bb8f0-157">Letztendlich können Änderungen an Ihrer APP zu wichtigen Änderungen erforderlich sein.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-157">Eventually changes to your app may require breaking changes.</span></span> <span data-ttu-id="bb8f0-158">Das Unterbrechen von alten Clients und das Erzwingen der Aktualisierung zusammen mit Ihrem Dienst sind keine guten Benutzer Möglichkeiten.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-158">Breaking old clients and forcing them to be updated along with your service isn't a good user experience.</span></span> <span data-ttu-id="bb8f0-159">Eine Möglichkeit, die Abwärtskompatibilität zu gewährleisten, während wichtige Änderungen vorgenommen werden, besteht in der Veröffentlichung mehrerer Versionen eines Dienstanbieter.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-159">A way to maintain backwards compatibility while making breaking changes is to publish multiple versions of a service.</span></span>

<span data-ttu-id="bb8f0-160">GrpC unterstützt einen optionalen [`package`](https://developers.google.com/protocol-buffers/docs/proto3#packages) Spezifizierer, der ähnlich wie ein .NET-Namespace funktioniert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-160">gRPC supports an optional [`package`](https://developers.google.com/protocol-buffers/docs/proto3#packages) specifier, which functions much like a .NET namespace.</span></span> <span data-ttu-id="bb8f0-161">Tatsächlich wird der `package` als .NET-Namespace für generierte .NET-Typen verwendet, wenn `option csharp_namespace` nicht in der *. proto* -Datei festgelegt ist.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-161">In fact the `package` will be used as the .NET namespace for generated .NET types if `option csharp_namespace` is not set in the *.proto* file.</span></span> <span data-ttu-id="bb8f0-162">Das Paket kann verwendet werden, um eine Versionsnummer für den Dienst und seine Nachrichten anzugeben:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-162">The package can be used to specify a version number for your service and its messages:</span></span>

[!code-protobuf[](versioning/sample/greet.v1.proto?highlight=3)]

<span data-ttu-id="bb8f0-163">Der Paketname wird mit dem Dienstnamen kombiniert, um eine Dienst Adresse zu identifizieren.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-163">The package name is combined with the service name to identify a service address.</span></span> <span data-ttu-id="bb8f0-164">Eine Dienst Adresse ermöglicht, dass mehrere Versionen eines Dienstanbieter nebeneinander gehostet werden:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-164">A service address allows multiple versions of a service to be hosted side-by-side:</span></span>

* `greet.v1.Greeter`
* `greet.v2.Greeter`

<span data-ttu-id="bb8f0-165">Implementierungen des versionierten Dienstanbieter werden in *Startup.cs*registriert:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-165">Implementations of the versioned service are registered in *Startup.cs*:</span></span>

```csharp
app.UseEndpoints(endpoints =>
{
    // Implements greet.v1.Greeter
    endpoints.MapGrpcService<GreeterServiceV1>();

    // Implements greet.v2.Greeter
    endpoints.MapGrpcService<GreeterServiceV2>();
});
```

<span data-ttu-id="bb8f0-166">Wenn Sie eine Versionsnummer in den Paketnamen einschließen, haben Sie die Möglichkeit, eine *v2* -Version Ihres Dienes mit wichtigen Änderungen zu veröffentlichen, während ältere Clients, die die *v1* -Version anrufen, weiterhin unterstützt werden.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-166">Including a version number in the package name gives you the opportunity to publish a *v2* version of your service with breaking changes, while continuing to support older clients who call the *v1* version.</span></span> <span data-ttu-id="bb8f0-167">Nachdem die Clients so aktualisiert wurden, dass Sie den *v2* -Dienst verwenden, können Sie die alte Version entfernen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-167">Once clients have updated to use the *v2* service you can choose to remove the old version.</span></span> <span data-ttu-id="bb8f0-168">Beim Planen der Veröffentlichung mehrerer Versionen eines Dienstanbieter:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-168">When planning to publish multiple versions of a service:</span></span>

- <span data-ttu-id="bb8f0-169">Vermeiden Sie bei angemessener Änderung wichtige Änderungen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-169">Avoid breaking changes if reasonable.</span></span>
- <span data-ttu-id="bb8f0-170">Aktualisieren Sie die Versionsnummer nicht, wenn Sie keine wichtigen Änderungen vornehmen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-170">Don't update the version number unless making breaking changes.</span></span>
- <span data-ttu-id="bb8f0-171">Aktualisieren Sie die Versionsnummer, wenn Sie wichtige Änderungen vornehmen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-171">Do update the version number when you make breaking changes.</span></span>

<span data-ttu-id="bb8f0-172">Durch das Veröffentlichen mehrerer Versionen eines dienstanvers wird dieses dupliziert.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-172">Publishing multiple versions of a service duplicates it.</span></span> <span data-ttu-id="bb8f0-173">Um die Duplizierung zu reduzieren, erwägen Sie, die Geschäftslogik von den Dienst Implementierungen an einen zentralisierten Speicherort zu verschieben, der von den alten und neuen Implementierungen verwendet werden kann:</span><span class="sxs-lookup"><span data-stu-id="bb8f0-173">To reduce duplication, consider moving business logic from the service implementations to a centralized location that can be reused by the old and new implementations:</span></span>

[!code-csharp[](versioning/sample/GreeterServiceV1.cs?highlight=10,19)]

<span data-ttu-id="bb8f0-174">Dienste und Nachrichten, die mit verschiedenen Paketnamen generiert werden, sind **unterschiedliche .NET-Typen**</span><span class="sxs-lookup"><span data-stu-id="bb8f0-174">Services and messages generated with different package names are **different .NET types**.</span></span> <span data-ttu-id="bb8f0-175">Das Verschieben von Geschäftslogik an einen zentralisierten Speicherort erfordert die Zuordnung von Nachrichten zu allgemeinen Typen.</span><span class="sxs-lookup"><span data-stu-id="bb8f0-175">Moving business logic to a centralized location requires mapping messages to common types.</span></span>
