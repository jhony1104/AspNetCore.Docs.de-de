---
title: Behandeln von Fehlern in ASP.net Core blazor-apps
author: guardrex
description: Erfahren Sie, wie ASP.net Core blazor, wie von blazor nicht behandelte Ausnahmen verwaltet werden, und wie Sie apps entwickeln, die Fehler erkennen und behandeln.
monikerRange: '>= aspnetcore-3.0'
ms.author: riande
ms.custom: mvc
ms.date: 10/31/2019
uid: blazor/handle-errors
ms.openlocfilehash: afcaa4d926c3e5f0a018897ce4b67b54574dae77
ms.sourcegitcommit: 77c8be22d5e88dd710f42c739748869f198865dd
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 11/01/2019
ms.locfileid: "73426985"
---
# <a name="handle-errors-in-aspnet-core-blazor-apps"></a><span data-ttu-id="885f6-103">Behandeln von Fehlern in ASP.net Core blazor-apps</span><span class="sxs-lookup"><span data-stu-id="885f6-103">Handle errors in ASP.NET Core Blazor apps</span></span>

<span data-ttu-id="885f6-104">Von [Steve Sanderson](https://github.com/SteveSandersonMS)</span><span class="sxs-lookup"><span data-stu-id="885f6-104">By [Steve Sanderson](https://github.com/SteveSandersonMS)</span></span>

<span data-ttu-id="885f6-105">In diesem Artikel wird beschrieben, wie blazor nicht behandelte Ausnahmen verwaltet und wie Sie apps entwickeln, die Fehler erkennen und behandeln.</span><span class="sxs-lookup"><span data-stu-id="885f6-105">This article describes how Blazor manages unhandled exceptions and how to develop apps that detect and handle errors.</span></span>

## <a name="how-the-blazor-framework-reacts-to-unhandled-exceptions"></a><span data-ttu-id="885f6-106">Reagieren des blazor-Frameworks auf nicht behandelte Ausnahmen</span><span class="sxs-lookup"><span data-stu-id="885f6-106">How the Blazor framework reacts to unhandled exceptions</span></span>

<span data-ttu-id="885f6-107">Der blazor-Server ist ein Zustands behaftetes Framework.</span><span class="sxs-lookup"><span data-stu-id="885f6-107">Blazor Server is a stateful framework.</span></span> <span data-ttu-id="885f6-108">Während Benutzer mit einer APP interagieren, behalten *Sie eine Verbindung*mit dem Server bei, der als Verbindung bezeichnet wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-108">While users interact with an app, they maintain a connection to the server known as a *circuit*.</span></span> <span data-ttu-id="885f6-109">Die Verbindung enthält aktive Komponenten Instanzen sowie viele andere Aspekte des Zustands, wie z. b.:</span><span class="sxs-lookup"><span data-stu-id="885f6-109">The circuit holds active component instances, plus many other aspects of state, such as:</span></span>

* <span data-ttu-id="885f6-110">Die letzte gerenderte Ausgabe von-Komponenten.</span><span class="sxs-lookup"><span data-stu-id="885f6-110">The most recent rendered output of components.</span></span>
* <span data-ttu-id="885f6-111">Die aktuelle Gruppe von Delegaten zur Ereignis Behandlung, die von Client seitigen Ereignissen ausgelöst werden könnten.</span><span class="sxs-lookup"><span data-stu-id="885f6-111">The current set of event-handling delegates that could be triggered by client-side events.</span></span>

<span data-ttu-id="885f6-112">Wenn ein Benutzer die APP auf mehreren Browser Registerkarten öffnet, sind mehrere unabhängige Verbindungen vorhanden.</span><span class="sxs-lookup"><span data-stu-id="885f6-112">If a user opens the app in multiple browser tabs, they have multiple independent circuits.</span></span>

<span data-ttu-id="885f6-113">Blazor behandelt die meisten nicht behandelten Ausnahmen als schwerwiegend an der Verbindung, in der Sie auftreten.</span><span class="sxs-lookup"><span data-stu-id="885f6-113">Blazor treats most unhandled exceptions as fatal to the circuit where they occur.</span></span> <span data-ttu-id="885f6-114">Wenn eine Verbindung aufgrund einer nicht behandelten Ausnahme beendet wird, kann der Benutzer die Interaktion mit der app nur fortsetzen, indem er die Seite erneut lädt, um eine neue Verbindung zu erstellen.</span><span class="sxs-lookup"><span data-stu-id="885f6-114">If a circuit is terminated due to an unhandled exception, the user can only continue to interact with the app by reloading the page to create a new circuit.</span></span> <span data-ttu-id="885f6-115">Verbindungen außerhalb der Beendigungs Seite, bei der es sich um Verbindungen für andere Benutzer oder andere Browser Registerkarten handelt, sind nicht betroffen.</span><span class="sxs-lookup"><span data-stu-id="885f6-115">Circuits outside of the one that's terminated, which are circuits for other users or other browser tabs, aren't affected.</span></span> <span data-ttu-id="885f6-116">Dieses Szenario ähnelt einer Desktop-App, die abstürzt,&mdash;die abstürzte APP neu gestartet werden muss, aber andere apps sind nicht betroffen.</span><span class="sxs-lookup"><span data-stu-id="885f6-116">This scenario is similar to a desktop app that crashes&mdash;the crashed app must be restarted, but other apps aren't affected.</span></span>

<span data-ttu-id="885f6-117">Eine Verbindung wird beendet, wenn eine nicht behandelte Ausnahme aus den folgenden Gründen auftritt:</span><span class="sxs-lookup"><span data-stu-id="885f6-117">A circuit is terminated when an unhandled exception occurs for the following reasons:</span></span>

* <span data-ttu-id="885f6-118">Eine nicht behandelte Ausnahme verlässt die Verbindung häufig in einem nicht definierten Zustand.</span><span class="sxs-lookup"><span data-stu-id="885f6-118">An unhandled exception often leaves the circuit in an undefined state.</span></span>
* <span data-ttu-id="885f6-119">Der normale Vorgang der APP kann nach einer nicht behandelten Ausnahme nicht garantiert werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-119">The app's normal operation can't be guaranteed after an unhandled exception.</span></span>
* <span data-ttu-id="885f6-120">Sicherheitsrisiken werden möglicherweise in der App angezeigt, wenn die Verbindung fortgesetzt wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-120">Security vulnerabilities may appear in the app if the circuit continues.</span></span>

## <a name="manage-unhandled-exceptions-in-developer-code"></a><span data-ttu-id="885f6-121">Verwalten von nicht behandelten Ausnahmen in Entwickler Code</span><span class="sxs-lookup"><span data-stu-id="885f6-121">Manage unhandled exceptions in developer code</span></span>

<span data-ttu-id="885f6-122">Damit eine APP nach einem Fehler fortgesetzt werden kann, muss die APP über eine Fehler Behandlungs Logik verfügen.</span><span class="sxs-lookup"><span data-stu-id="885f6-122">For an app to continue after an error, the app must have error handling logic.</span></span> <span data-ttu-id="885f6-123">In späteren Abschnitten dieses Artikels werden mögliche Quellen für nicht behandelte Ausnahmen beschrieben.</span><span class="sxs-lookup"><span data-stu-id="885f6-123">Later sections of this article describe potential sources of unhandled exceptions.</span></span>

<span data-ttu-id="885f6-124">In der Produktionsumgebung müssen Sie keine Framework-Ausnahme Meldungen oder Stapel Überwachungen in der Benutzeroberfläche erstellen.</span><span class="sxs-lookup"><span data-stu-id="885f6-124">In production, don't render framework exception messages or stack traces in the UI.</span></span> <span data-ttu-id="885f6-125">Das Rendern von Ausnahme Meldungen oder Stapel Überwachungen könnte:</span><span class="sxs-lookup"><span data-stu-id="885f6-125">Rendering exception messages or stack traces could:</span></span>

* <span data-ttu-id="885f6-126">Geben Sie den Endbenutzern vertrauliche Informationen offen.</span><span class="sxs-lookup"><span data-stu-id="885f6-126">Disclose sensitive information to end users.</span></span>
* <span data-ttu-id="885f6-127">Helfen Sie einem böswilligen Benutzer dabei, Schwachstellen in einer APP zu erkennen, die die Sicherheit der APP, des Servers oder des Netzwerks beeinträchtigen können.</span><span class="sxs-lookup"><span data-stu-id="885f6-127">Help a malicious user discover weaknesses in an app that can compromise the security of the app, server, or network.</span></span>

## <a name="log-errors-with-a-persistent-provider"></a><span data-ttu-id="885f6-128">Protokollieren von Fehlern mit einem permanenten Anbieter</span><span class="sxs-lookup"><span data-stu-id="885f6-128">Log errors with a persistent provider</span></span>

<span data-ttu-id="885f6-129">Wenn eine nicht behandelte Ausnahme auftritt, wird die Ausnahme in <xref:Microsoft.Extensions.Logging.ILogger> Instanzen protokolliert, die im Dienst Container konfiguriert sind.</span><span class="sxs-lookup"><span data-stu-id="885f6-129">If an unhandled exception occurs, the exception is logged to <xref:Microsoft.Extensions.Logging.ILogger> instances configured in the service container.</span></span> <span data-ttu-id="885f6-130">Standardmäßig melden sich blazor-apps mit dem Konsolen Protokollierungs Anbieter an der Konsolenausgabe an.</span><span class="sxs-lookup"><span data-stu-id="885f6-130">By default, Blazor apps log to console output with the Console Logging Provider.</span></span> <span data-ttu-id="885f6-131">Melden Sie sich bei einem Anbieter, der die Protokoll Größe und die Protokoll Rotation verwaltet, an einem permanenten Speicherort an.</span><span class="sxs-lookup"><span data-stu-id="885f6-131">Consider logging to a more permanent location with a provider that manages log size and log rotation.</span></span> <span data-ttu-id="885f6-132">Weitere Informationen finden Sie unter <xref:fundamentals/logging/index>.</span><span class="sxs-lookup"><span data-stu-id="885f6-132">For more information, see <xref:fundamentals/logging/index>.</span></span>

<span data-ttu-id="885f6-133">Während der Entwicklung sendet blazor in der Regel die vollständigen Details der Ausnahmen an die Konsole des Browsers, um das Debuggen zu unterstützen.</span><span class="sxs-lookup"><span data-stu-id="885f6-133">During development, Blazor usually sends the full details of exceptions to the browser's console to aid in debugging.</span></span> <span data-ttu-id="885f6-134">In der Produktionsumgebung sind ausführliche Fehler in der Browser Konsole standardmäßig deaktiviert. Dies bedeutet, dass Fehler nicht an Clients gesendet werden, sondern dass die vollständigen Details der Ausnahme weiterhin serverseitig protokolliert werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-134">In production, detailed errors in the browser's console are disabled by default, which means that errors aren't sent to clients but the exception's full details are still logged server-side.</span></span> <span data-ttu-id="885f6-135">Weitere Informationen finden Sie unter <xref:fundamentals/error-handling>.</span><span class="sxs-lookup"><span data-stu-id="885f6-135">For more information, see <xref:fundamentals/error-handling>.</span></span>

<span data-ttu-id="885f6-136">Sie müssen entscheiden, welche Vorfälle protokolliert werden sollen, und den Schweregrad protokollierter Vorfälle.</span><span class="sxs-lookup"><span data-stu-id="885f6-136">You must decide which incidents to log and the level of severity of logged incidents.</span></span> <span data-ttu-id="885f6-137">Feindliche Benutzer können möglicherweise Fehler absichtlich auslöst.</span><span class="sxs-lookup"><span data-stu-id="885f6-137">Hostile users might be able to trigger errors deliberately.</span></span> <span data-ttu-id="885f6-138">Beispielsweise können Sie keinen Incident von einem Fehler protokollieren, bei dem in der URL einer Komponente, die Produktdetails anzeigt, ein unbekannter `ProductId` angegeben wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-138">For example, don't log an incident from an error where an unknown `ProductId` is supplied in the URL of a component that displays product details.</span></span> <span data-ttu-id="885f6-139">Nicht alle Fehler sollten als Vorfälle mit hohem Schweregrad für die Protokollierung behandelt werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-139">Not all errors should be treated as high-severity incidents for logging.</span></span>

## <a name="places-where-errors-may-occur"></a><span data-ttu-id="885f6-140">Orte, an denen Fehler auftreten können</span><span class="sxs-lookup"><span data-stu-id="885f6-140">Places where errors may occur</span></span>

<span data-ttu-id="885f6-141">Framework-und app-Code können nicht behandelte Ausnahmen an einem der folgenden Speicherorte auslöst:</span><span class="sxs-lookup"><span data-stu-id="885f6-141">Framework and app code may trigger unhandled exceptions in any of the following locations:</span></span>

* [<span data-ttu-id="885f6-142">Komponenten Instanziierung</span><span class="sxs-lookup"><span data-stu-id="885f6-142">Component instantiation</span></span>](#component-instantiation)
* [<span data-ttu-id="885f6-143">Lebenszyklus Methoden</span><span class="sxs-lookup"><span data-stu-id="885f6-143">Lifecycle methods</span></span>](#lifecycle-methods)
* [<span data-ttu-id="885f6-144">Renderinglogik</span><span class="sxs-lookup"><span data-stu-id="885f6-144">Rendering logic</span></span>](#rendering-logic)
* [<span data-ttu-id="885f6-145">Ereignishandler</span><span class="sxs-lookup"><span data-stu-id="885f6-145">Event handlers</span></span>](#event-handlers)
* [<span data-ttu-id="885f6-146">Komponenten Beseitigung</span><span class="sxs-lookup"><span data-stu-id="885f6-146">Component disposal</span></span>](#component-disposal)
* [<span data-ttu-id="885f6-147">JavaScript-Interop</span><span class="sxs-lookup"><span data-stu-id="885f6-147">JavaScript interop</span></span>](#javascript-interop)
* [<span data-ttu-id="885f6-148">Verbindungs Handler</span><span class="sxs-lookup"><span data-stu-id="885f6-148">Circuit handlers</span></span>](#circuit-handlers)
* [<span data-ttu-id="885f6-149">Freigabe Entfernung</span><span class="sxs-lookup"><span data-stu-id="885f6-149">Circuit disposal</span></span>](#circuit-disposal)
* [<span data-ttu-id="885f6-150">Wird vorab durchgeführt</span><span class="sxs-lookup"><span data-stu-id="885f6-150">Prerendering</span></span>](#prerendering)

<span data-ttu-id="885f6-151">Die vorangegangenen nicht behandelten Ausnahmen werden in den folgenden Abschnitten dieses Artikels beschrieben.</span><span class="sxs-lookup"><span data-stu-id="885f6-151">The preceding unhandled exceptions are described in the following sections of this article.</span></span>

### <a name="component-instantiation"></a><span data-ttu-id="885f6-152">Komponenten Instanziierung</span><span class="sxs-lookup"><span data-stu-id="885f6-152">Component instantiation</span></span>

<span data-ttu-id="885f6-153">Wenn blazor eine Instanz einer Komponente erstellt:</span><span class="sxs-lookup"><span data-stu-id="885f6-153">When Blazor creates an instance of a component:</span></span>

* <span data-ttu-id="885f6-154">Der Konstruktor der Komponente wird aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="885f6-154">The component's constructor is invoked.</span></span>
* <span data-ttu-id="885f6-155">Die Konstruktoren aller nicht-Singleton-di-Dienste, die über die [@inject](xref:blazor/dependency-injection#request-a-service-in-a-component) -Direktive oder das [[einschleusen]](xref:blazor/dependency-injection#request-a-service-in-a-component) -Attribut für den Konstruktor der Komponente bereitgestellt werden, werden aufgerufen.</span><span class="sxs-lookup"><span data-stu-id="885f6-155">The constructors of any non-singleton DI services supplied to the component's constructor via the [@inject](xref:blazor/dependency-injection#request-a-service-in-a-component) directive or the [[Inject]](xref:blazor/dependency-injection#request-a-service-in-a-component) attribute are invoked.</span></span> 

<span data-ttu-id="885f6-156">Eine Verbindung kann nicht hergestellt werden, wenn ein ausgeführter Konstruktor oder ein Setter für eine `[Inject]` Eigenschaft eine nicht behandelte Ausnahme auslöst.</span><span class="sxs-lookup"><span data-stu-id="885f6-156">A circuit fails when any executed constructor or a setter for any `[Inject]` property throws an unhandled exception.</span></span> <span data-ttu-id="885f6-157">Die Ausnahme ist schwerwiegend, da das Framework die Komponente nicht instanziieren kann.</span><span class="sxs-lookup"><span data-stu-id="885f6-157">The exception is fatal because the framework can't instantiate the component.</span></span> <span data-ttu-id="885f6-158">Wenn die Konstruktorlogik Ausnahmen auslösen kann, sollte die APP die Ausnahmen mithilfe einer [try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisung mit Fehlerbehandlung und Protokollierung abfangen.</span><span class="sxs-lookup"><span data-stu-id="885f6-158">If constructor logic may throw exceptions, the app should trap the exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

### <a name="lifecycle-methods"></a><span data-ttu-id="885f6-159">Lebenszyklusmethoden</span><span class="sxs-lookup"><span data-stu-id="885f6-159">Lifecycle methods</span></span>

<span data-ttu-id="885f6-160">Während der Lebensdauer einer Komponente ruft blazor Lebenszyklus Methoden auf:</span><span class="sxs-lookup"><span data-stu-id="885f6-160">During the lifetime of a component, Blazor invokes lifecycle methods:</span></span>

* `OnInitialized` / `OnInitializedAsync`
* `OnParametersSet` / `OnParametersSetAsync`
* `ShouldRender` / `ShouldRenderAsync`
* `OnAfterRender` / `OnAfterRenderAsync`

<span data-ttu-id="885f6-161">Wenn eine Lebenszyklus Methode eine Ausnahme synchron oder asynchron auslöst, ist die Ausnahme für die Verbindung schwerwiegend.</span><span class="sxs-lookup"><span data-stu-id="885f6-161">If any lifecycle method throws an exception, synchronously or asynchronously, the exception is fatal to the circuit.</span></span> <span data-ttu-id="885f6-162">Fügen Sie Fehler Behandlungs Logik hinzu, damit Komponenten Fehler in Lebenszyklus Methoden behandeln können.</span><span class="sxs-lookup"><span data-stu-id="885f6-162">For components to deal with errors in lifecycle methods, add error handling logic.</span></span>

<span data-ttu-id="885f6-163">Im folgenden Beispiel, in dem `OnParametersSetAsync` eine Methode zum Abrufen eines Produkts aufruft:</span><span class="sxs-lookup"><span data-stu-id="885f6-163">In the following example where `OnParametersSetAsync` calls a method to obtain a product:</span></span>

* <span data-ttu-id="885f6-164">Eine Ausnahme, die in der `ProductRepository.GetProductByIdAsync`-Methode ausgelöst wird, wird von einer `try-catch`-Anweisung behandelt.</span><span class="sxs-lookup"><span data-stu-id="885f6-164">An exception thrown in the `ProductRepository.GetProductByIdAsync` method is handled by a `try-catch` statement.</span></span>
* <span data-ttu-id="885f6-165">Wenn der `catch`-Block ausgeführt wird:</span><span class="sxs-lookup"><span data-stu-id="885f6-165">When the `catch` block is executed:</span></span>
  * <span data-ttu-id="885f6-166">`loadFailed` ist auf `true`festgelegt, das verwendet wird, um dem Benutzer eine Fehlermeldung anzuzeigen.</span><span class="sxs-lookup"><span data-stu-id="885f6-166">`loadFailed` is set to `true`, which is used to display an error message to the user.</span></span>
  * <span data-ttu-id="885f6-167">Der Fehler wird protokolliert.</span><span class="sxs-lookup"><span data-stu-id="885f6-167">The error is logged.</span></span>

[!code-cshtml[](handle-errors/samples_snapshot/3.x/product-details.razor?highlight=11,27-39)]

### <a name="rendering-logic"></a><span data-ttu-id="885f6-168">Renderinglogik</span><span class="sxs-lookup"><span data-stu-id="885f6-168">Rendering logic</span></span>

<span data-ttu-id="885f6-169">Das deklarative Markup in einer `.razor` Komponenten Datei wird in eine C# Methode namens `BuildRenderTree`kompiliert.</span><span class="sxs-lookup"><span data-stu-id="885f6-169">The declarative markup in a `.razor` component file is compiled into a C# method called `BuildRenderTree`.</span></span> <span data-ttu-id="885f6-170">Wenn eine Komponente gerendert wird, wird `BuildRenderTree` ausgeführt und erstellt eine Datenstruktur, die die Elemente, den Text und die untergeordneten Komponenten der gerenderten Komponente beschreibt.</span><span class="sxs-lookup"><span data-stu-id="885f6-170">When a component renders, `BuildRenderTree` executes and builds up a data structure describing the elements, text, and child components of the rendered component.</span></span>

<span data-ttu-id="885f6-171">Die Renderinglogik kann eine Ausnahme auslösen.</span><span class="sxs-lookup"><span data-stu-id="885f6-171">Rendering logic can throw an exception.</span></span> <span data-ttu-id="885f6-172">Ein Beispiel für dieses Szenario tritt auf, wenn `@someObject.PropertyName` ausgewertet, aber `@someObject` `null`ist.</span><span class="sxs-lookup"><span data-stu-id="885f6-172">An example of this scenario occurs when `@someObject.PropertyName` is evaluated but `@someObject` is `null`.</span></span> <span data-ttu-id="885f6-173">Eine nicht behandelte Ausnahme, die von der Renderinglogik ausgelöst wird, ist schwerwiegend auf die</span><span class="sxs-lookup"><span data-stu-id="885f6-173">An unhandled exception thrown by rendering logic is fatal to the circuit.</span></span>

<span data-ttu-id="885f6-174">Um eine NULL-Verweis Ausnahme in der Renderingerweiterung zu vermeiden, suchen Sie vor dem Zugriff auf die Member nach einem `null` Objekt.</span><span class="sxs-lookup"><span data-stu-id="885f6-174">To prevent a null reference exception in rendering logic, check for a `null` object before accessing its members.</span></span> <span data-ttu-id="885f6-175">Im folgenden Beispiel wird auf `person.Address` Eigenschaften nicht zugegriffen, wenn `person.Address` `null`ist:</span><span class="sxs-lookup"><span data-stu-id="885f6-175">In the following example, `person.Address` properties aren't accessed if `person.Address` is `null`:</span></span>

[!code-cshtml[](handle-errors/samples_snapshot/3.x/person-example.razor?highlight=1)]

<span data-ttu-id="885f6-176">Der vorangehende Code geht davon aus, dass `person` nicht `null`ist.</span><span class="sxs-lookup"><span data-stu-id="885f6-176">The preceding code assumes that `person` isn't `null`.</span></span> <span data-ttu-id="885f6-177">Häufig gewährleistet die Struktur des Codes, dass ein Objekt vorhanden ist, wenn die Komponente gerendert wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-177">Often, the structure of the code guarantees that an object exists at the time the component is rendered.</span></span> <span data-ttu-id="885f6-178">In diesen Fällen ist es nicht notwendig, auf `null` in der Renderinglogik zu prüfen.</span><span class="sxs-lookup"><span data-stu-id="885f6-178">In those cases, it isn't necessary to check for `null` in rendering logic.</span></span> <span data-ttu-id="885f6-179">Im vorherigen Beispiel ist möglicherweise gewährleistet, dass `person` vorhanden ist, da `person` erstellt wird, wenn die Komponente instanziiert wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-179">In the prior example, `person` might be guaranteed to exist because `person` is created when the component is instantiated.</span></span>

### <a name="event-handlers"></a><span data-ttu-id="885f6-180">Ereignishandler</span><span class="sxs-lookup"><span data-stu-id="885f6-180">Event handlers</span></span>

<span data-ttu-id="885f6-181">Client seitiger Code löst Aufrufe von Code aus C# , wenn Ereignishandler mit erstellt werden:</span><span class="sxs-lookup"><span data-stu-id="885f6-181">Client-side code triggers invocations of C# code when event handlers are created using:</span></span>

* `@onclick`
* `@onchange`
* <span data-ttu-id="885f6-182">Weitere `@on...` Attribute</span><span class="sxs-lookup"><span data-stu-id="885f6-182">Other `@on...` attributes</span></span>
* `@bind`

<span data-ttu-id="885f6-183">Der Ereignishandlercode löst in diesen Szenarien möglicherweise eine nicht behandelte Ausnahme aus.</span><span class="sxs-lookup"><span data-stu-id="885f6-183">Event handler code might throw an unhandled exception in these scenarios.</span></span>

<span data-ttu-id="885f6-184">Wenn ein Ereignishandler eine nicht behandelte Ausnahme auslöst (z. b. Wenn eine Datenbankabfrage fehlschlägt), ist die Ausnahme für die Verbindung schwerwiegend.</span><span class="sxs-lookup"><span data-stu-id="885f6-184">If an event handler throws an unhandled exception (for example, a database query fails), the exception is fatal to the circuit.</span></span> <span data-ttu-id="885f6-185">Wenn die app Code aufruft, der aus externen Gründen fehlschlagen könnte, fangen Sie Ausnahmen mithilfe einer [try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisung mit Fehlerbehandlung und Protokollierung ab.</span><span class="sxs-lookup"><span data-stu-id="885f6-185">If the app calls code that could fail for external reasons, trap exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

<span data-ttu-id="885f6-186">Wenn Benutzercode die Ausnahme nicht abfängt und behandelt, protokolliert das Framework die Ausnahme und beendet die Verbindung.</span><span class="sxs-lookup"><span data-stu-id="885f6-186">If user code doesn't trap and handle the exception, the framework logs the exception and terminates the circuit.</span></span>

### <a name="component-disposal"></a><span data-ttu-id="885f6-187">Komponenten Beseitigung</span><span class="sxs-lookup"><span data-stu-id="885f6-187">Component disposal</span></span>

<span data-ttu-id="885f6-188">Eine Komponente kann z. b. aus der Benutzeroberfläche entfernt werden, weil der Benutzer zu einer anderen Seite navigiert ist.</span><span class="sxs-lookup"><span data-stu-id="885f6-188">A component may be removed from the UI, for example, because the user has navigated to another page.</span></span> <span data-ttu-id="885f6-189">Wenn eine Komponente, die <xref:System.IDisposable?displayProperty=fullName> implementiert, von der Benutzeroberfläche entfernt wird, ruft das Framework die <xref:System.IDisposable.Dispose*> Methode der Komponente auf.</span><span class="sxs-lookup"><span data-stu-id="885f6-189">When a component that implements <xref:System.IDisposable?displayProperty=fullName> is removed from the UI, the framework calls the component's <xref:System.IDisposable.Dispose*> method.</span></span> 

<span data-ttu-id="885f6-190">Wenn die `Dispose`-Methode der Komponente eine nicht behandelte Ausnahme auslöst, ist die Ausnahme für die Verbindung schwerwiegend.</span><span class="sxs-lookup"><span data-stu-id="885f6-190">If the component's `Dispose` method throws an unhandled exception, the exception is fatal to the circuit.</span></span> <span data-ttu-id="885f6-191">Wenn die Entsorgungs Logik Ausnahmen auslösen kann, sollte die APP die Ausnahmen mithilfe einer [try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisung mit Fehlerbehandlung und Protokollierung abfangen.</span><span class="sxs-lookup"><span data-stu-id="885f6-191">If disposal logic may throw exceptions, the app should trap the exceptions using a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span>

<span data-ttu-id="885f6-192">Weitere Informationen zur Komponenten Beseitigung finden Sie unter <xref:blazor/components#component-disposal-with-idisposable>.</span><span class="sxs-lookup"><span data-stu-id="885f6-192">For more information on component disposal, see <xref:blazor/components#component-disposal-with-idisposable>.</span></span>

### <a name="javascript-interop"></a><span data-ttu-id="885f6-193">JavaScript-Interoperabilität</span><span class="sxs-lookup"><span data-stu-id="885f6-193">JavaScript interop</span></span>

<span data-ttu-id="885f6-194">`IJSRuntime.InvokeAsync<T>` ermöglicht .NET-Code das Ausführen von asynchronen Aufrufen der JavaScript-Laufzeit im Browser des Benutzers.</span><span class="sxs-lookup"><span data-stu-id="885f6-194">`IJSRuntime.InvokeAsync<T>` allows .NET code to make asynchronous calls to the JavaScript runtime in the user's browser.</span></span>

<span data-ttu-id="885f6-195">Die folgenden Bedingungen gelten für die Fehlerbehandlung mit `InvokeAsync<T>`:</span><span class="sxs-lookup"><span data-stu-id="885f6-195">The following conditions apply to error handling with `InvokeAsync<T>`:</span></span>

* <span data-ttu-id="885f6-196">Wenn bei einem Rückruf von `InvokeAsync<T>` synchron ein Fehler auftritt, wird eine .NET-Ausnahme ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="885f6-196">If a call to `InvokeAsync<T>` fails synchronously, a .NET exception occurs.</span></span> <span data-ttu-id="885f6-197">Ein-`InvokeAsync<T>` kann beispielsweise fehlschlagen, da die bereitgestellten Argumente nicht serialisiert werden können.</span><span class="sxs-lookup"><span data-stu-id="885f6-197">A call to `InvokeAsync<T>` may fail, for example, because the supplied arguments can't be serialized.</span></span> <span data-ttu-id="885f6-198">Der Entwickler Code muss die Ausnahme abfangen.</span><span class="sxs-lookup"><span data-stu-id="885f6-198">Developer code must catch the exception.</span></span> <span data-ttu-id="885f6-199">Wenn der app-Code in einem Ereignishandler oder einer Komponenten Lebenszyklus-Methode keine Ausnahme behandelt, ist die resultierende Ausnahme schwerwiegend auf die Verbindung.</span><span class="sxs-lookup"><span data-stu-id="885f6-199">If app code in an event handler or component lifecycle method doesn't handle an exception, the resulting exception is fatal to the circuit.</span></span>
* <span data-ttu-id="885f6-200">Wenn ein-`InvokeAsync<T>` asynchron fehlschlägt, kann die .net-<xref:System.Threading.Tasks.Task> nicht ausgeführt werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-200">If a call to `InvokeAsync<T>` fails asynchronously, the .NET <xref:System.Threading.Tasks.Task> fails.</span></span> <span data-ttu-id="885f6-201">Ein-`InvokeAsync<T>` kann beispielsweise fehlschlagen, weil der JavaScript-seitige Code eine Ausnahme auslöst oder eine `Promise` zurückgibt, die als `rejected`abgeschlossen wurde.</span><span class="sxs-lookup"><span data-stu-id="885f6-201">A call to `InvokeAsync<T>` may fail, for example, because the JavaScript-side code throws an exception or returns a `Promise` that completed as `rejected`.</span></span> <span data-ttu-id="885f6-202">Der Entwickler Code muss die Ausnahme abfangen.</span><span class="sxs-lookup"><span data-stu-id="885f6-202">Developer code must catch the exception.</span></span> <span data-ttu-id="885f6-203">Wenn Sie den [Erwartungs Operator verwenden](/dotnet/csharp/language-reference/keywords/await) , sollten Sie den Methoden aufrufin eine [try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisung mit Fehlerbehandlung und Protokollierung umhüllen.</span><span class="sxs-lookup"><span data-stu-id="885f6-203">If using the [await](/dotnet/csharp/language-reference/keywords/await) operator, consider wrapping the method call in a [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statement with error handling and logging.</span></span> <span data-ttu-id="885f6-204">Andernfalls führt der fehlgeschlagene Code zu einer nicht behandelten Ausnahme, die für die Verbindung schwerwiegend ist.</span><span class="sxs-lookup"><span data-stu-id="885f6-204">Otherwise, the failing code results in an unhandled exception that's fatal to the circuit.</span></span>
* <span data-ttu-id="885f6-205">Standardmäßig müssen Aufrufe von `InvokeAsync<T>` innerhalb eines bestimmten Zeitraums ausgeführt werden, oder für den Aufruf wird ein Timeout festgestellt. Der Standard Timeout Zeitraum beträgt eine Minute.</span><span class="sxs-lookup"><span data-stu-id="885f6-205">By default, calls to `InvokeAsync<T>` must complete within a certain period or else the call times out. The default timeout period is one minute.</span></span> <span data-ttu-id="885f6-206">Das Timeout schützt den Code vor einem Verlust in Netzwerk Konnektivität oder JavaScript-Code, der niemals eine Abschluss Nachricht zurücksendet.</span><span class="sxs-lookup"><span data-stu-id="885f6-206">The timeout protects the code against a loss in network connectivity or JavaScript code that never sends back a completion message.</span></span> <span data-ttu-id="885f6-207">Wenn für den-Rückruf ein Timeout auftritt, tritt bei der resultierenden `Task` ein <xref:System.OperationCanceledException>auf.</span><span class="sxs-lookup"><span data-stu-id="885f6-207">If the call times out, the resulting `Task` fails with an <xref:System.OperationCanceledException>.</span></span> <span data-ttu-id="885f6-208">Trap und verarbeitet die Ausnahme mit der Protokollierung.</span><span class="sxs-lookup"><span data-stu-id="885f6-208">Trap and process the exception with logging.</span></span>

<span data-ttu-id="885f6-209">Ebenso kann JavaScript-Code Aufrufe von .NET-Methoden initiieren, die durch das [[jsinvokable]-Attribut](xref:blazor/javascript-interop#invoke-net-methods-from-javascript-functions)angegeben werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-209">Similarly, JavaScript code may initiate calls to .NET methods indicated by the [[JSInvokable] attribute](xref:blazor/javascript-interop#invoke-net-methods-from-javascript-functions).</span></span> <span data-ttu-id="885f6-210">Wenn diese .NET-Methoden eine nicht behandelte Ausnahme auslösen:</span><span class="sxs-lookup"><span data-stu-id="885f6-210">If these .NET methods throw an unhandled exception:</span></span>

* <span data-ttu-id="885f6-211">Die Ausnahme wird nicht als schwerwiegender Fehler für die Verbindung behandelt.</span><span class="sxs-lookup"><span data-stu-id="885f6-211">The exception isn't treated as fatal to the circuit.</span></span>
* <span data-ttu-id="885f6-212">Die JavaScript-seitige `Promise` wird zurückgewiesen.</span><span class="sxs-lookup"><span data-stu-id="885f6-212">The JavaScript-side `Promise` is rejected.</span></span>

<span data-ttu-id="885f6-213">Sie haben die Möglichkeit, Fehler Behandlungs Code entweder auf der .NET-Seite oder auf der JavaScript-Seite des Methoden Aufrufes zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="885f6-213">You have the option of using error handling code on either the .NET side or the JavaScript side of the method call.</span></span>

<span data-ttu-id="885f6-214">Weitere Informationen finden Sie unter <xref:blazor/javascript-interop>.</span><span class="sxs-lookup"><span data-stu-id="885f6-214">For more information, see <xref:blazor/javascript-interop>.</span></span>

### <a name="circuit-handlers"></a><span data-ttu-id="885f6-215">Verbindungs Handler</span><span class="sxs-lookup"><span data-stu-id="885f6-215">Circuit handlers</span></span>

<span data-ttu-id="885f6-216">Mit blazor kann Code einen Verbindungs *Handler*definieren, der Benachrichtigungen empfängt, wenn sich der Status der Verbindung eines Benutzers ändert.</span><span class="sxs-lookup"><span data-stu-id="885f6-216">Blazor allows code to define a *circuit handler*, which receives notifications when the state of a user's circuit changes.</span></span> <span data-ttu-id="885f6-217">Die folgenden Zustände werden verwendet:</span><span class="sxs-lookup"><span data-stu-id="885f6-217">The following states are used:</span></span>

* `initialized`
* `connected`
* `disconnected`
* `disposed`

<span data-ttu-id="885f6-218">Benachrichtigungen werden durch Registrieren eines di-Dienstanbieter verwaltet, der von der `CircuitHandler` abstrakten Basisklasse erbt.</span><span class="sxs-lookup"><span data-stu-id="885f6-218">Notifications are managed by registering a DI service that inherits from the `CircuitHandler` abstract base class.</span></span>

<span data-ttu-id="885f6-219">Wenn die Methoden eines benutzerdefinierten Verbindungs Handlers eine nicht behandelte Ausnahme auslösen, ist die Ausnahme für die Verbindung schwerwiegend.</span><span class="sxs-lookup"><span data-stu-id="885f6-219">If a custom circuit handler's methods throw an unhandled exception, the exception is fatal to the circuit.</span></span> <span data-ttu-id="885f6-220">Um Ausnahmen im Code eines Handlers zu tolerieren oder Methoden aufzurufen, packen Sie den Code in einer oder mehreren [try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisungen mit Fehlerbehandlung und Protokollierung.</span><span class="sxs-lookup"><span data-stu-id="885f6-220">To tolerate exceptions in a handler's code or called methods, wrap the code in one or more [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statements with error handling and logging.</span></span>

### <a name="circuit-disposal"></a><span data-ttu-id="885f6-221">Freigabe Entfernung</span><span class="sxs-lookup"><span data-stu-id="885f6-221">Circuit disposal</span></span>

<span data-ttu-id="885f6-222">Wenn eine Verbindung beendet wird, weil ein Benutzer die Verbindung getrennt hat und das Framework den Verbindungsstatus bereinigt, gibt das Framework den di-Bereich der Verbindung frei.</span><span class="sxs-lookup"><span data-stu-id="885f6-222">When a circuit ends because a user has disconnected and the framework is cleaning up the circuit state, the framework disposes of the circuit's DI scope.</span></span> <span data-ttu-id="885f6-223">Wenn Sie den Bereich verwerfen, werden alle di-Dienste, die mit <xref:System.IDisposable?displayProperty=fullName>verbunden sind, mit dem Bereich entfernt.</span><span class="sxs-lookup"><span data-stu-id="885f6-223">Disposing the scope disposes any circuit-scoped DI services that implement <xref:System.IDisposable?displayProperty=fullName>.</span></span> <span data-ttu-id="885f6-224">Wenn ein di-Dienst während der Entfernung eine nicht behandelte Ausnahme auslöst, protokolliert das Framework die Ausnahme.</span><span class="sxs-lookup"><span data-stu-id="885f6-224">If any DI service throws an unhandled exception during disposal, the framework logs the exception.</span></span>

### <a name="prerendering"></a><span data-ttu-id="885f6-225">Wird vorab durchgeführt</span><span class="sxs-lookup"><span data-stu-id="885f6-225">Prerendering</span></span>

<span data-ttu-id="885f6-226">Blazor-Komponenten können mithilfe von `Html.RenderComponentAsync` vorab übernommen werden, damit das gerenderte HTML-Markup als Teil der ursprünglichen HTTP-Anforderung des Benutzers zurückgegeben wird.</span><span class="sxs-lookup"><span data-stu-id="885f6-226">Blazor components can be prerendered using `Html.RenderComponentAsync` so that their rendered HTML markup is returned as part of the user's initial HTTP request.</span></span> <span data-ttu-id="885f6-227">Dies funktioniert wie folgt:</span><span class="sxs-lookup"><span data-stu-id="885f6-227">This works by:</span></span>

* <span data-ttu-id="885f6-228">Erstellen einer neuen Verbindung für alle vorab erstellten Komponenten, die Teil der gleichen Seite sind.</span><span class="sxs-lookup"><span data-stu-id="885f6-228">Creating a new circuit for all of the prerendered components that are part of the same page.</span></span>
* <span data-ttu-id="885f6-229">Das anfängliche HTML wird erzeugt.</span><span class="sxs-lookup"><span data-stu-id="885f6-229">Generating the initial HTML.</span></span>
* <span data-ttu-id="885f6-230">Die Verbindung wird als `disconnected` behandelt, bis der Browser des Benutzers eine signalr-Verbindung zum gleichen Server herstellt.</span><span class="sxs-lookup"><span data-stu-id="885f6-230">Treating the circuit as `disconnected` until the user's browser establishes a SignalR connection back to the same server.</span></span> <span data-ttu-id="885f6-231">Wenn die Verbindung hergestellt wird, wird die Interaktivität der Verbindung fortgesetzt, und das HTML-Markup der Komponenten wird aktualisiert.</span><span class="sxs-lookup"><span data-stu-id="885f6-231">When the connection is established, interactivity on the circuit is resumed and the components' HTML markup is updated.</span></span>

<span data-ttu-id="885f6-232">Wenn eine Komponente während der vorab Generierung eine nicht behandelte Ausnahme auslöst, z. b. während einer Lebenszyklus Methode oder in Renderinglogik:</span><span class="sxs-lookup"><span data-stu-id="885f6-232">If any component throws an unhandled exception during prerendering, for example, during a lifecycle method or in rendering logic:</span></span>

* <span data-ttu-id="885f6-233">Die Ausnahme ist für die Verbindung schwerwiegend.</span><span class="sxs-lookup"><span data-stu-id="885f6-233">The exception is fatal to the circuit.</span></span>
* <span data-ttu-id="885f6-234">Die Ausnahme wird in der aufrufsstapel aus dem `Html.RenderComponentAsync`-Befehl ausgelöst.</span><span class="sxs-lookup"><span data-stu-id="885f6-234">The exception is thrown up the call stack from the `Html.RenderComponentAsync` call.</span></span> <span data-ttu-id="885f6-235">Daher schlägt die gesamte HTTP-Anforderung fehl, es sei denn, die Ausnahme wird explizit durch den Entwickler Code abgefangen.</span><span class="sxs-lookup"><span data-stu-id="885f6-235">Therefore, the entire HTTP request fails unless the exception is explicitly caught by developer code.</span></span>

<span data-ttu-id="885f6-236">Unter normalen Umständen ist es nicht sinnvoll, die Komponente zu erstellen und zu rendern, da eine funktionierende Komponente nicht gerendert werden kann.</span><span class="sxs-lookup"><span data-stu-id="885f6-236">Under normal circumstances when prerendering fails, continuing to build and render the component doesn't make sense because a working component can't be rendered.</span></span>

<span data-ttu-id="885f6-237">Um Fehler zu tolerieren, die möglicherweise während der vorab Generierung auftreten, muss die Fehler Behandlungs Logik in einer Komponente platziert werden, die Ausnahmen auslösen kann.</span><span class="sxs-lookup"><span data-stu-id="885f6-237">To tolerate errors that may occur during prerendering, error handling logic must be placed inside a component that may throw exceptions.</span></span> <span data-ttu-id="885f6-238">Verwenden [Sie try-catch-](/dotnet/csharp/language-reference/keywords/try-catch) Anweisungen mit Fehlerbehandlung und Protokollierung.</span><span class="sxs-lookup"><span data-stu-id="885f6-238">Use [try-catch](/dotnet/csharp/language-reference/keywords/try-catch) statements with error handling and logging.</span></span> <span data-ttu-id="885f6-239">Anstatt den aufzurufenden `RenderComponentAsync` in einer `try-catch`-Anweisung zu umwickeln, platzieren Sie die Fehler Behandlungs Logik in der von `RenderComponentAsync`gerenderten Komponente.</span><span class="sxs-lookup"><span data-stu-id="885f6-239">Instead of wrapping the call to `RenderComponentAsync` in a `try-catch` statement, place error handling logic in the component rendered by `RenderComponentAsync`.</span></span>

## <a name="advanced-scenarios"></a><span data-ttu-id="885f6-240">Erweiterte Szenarien</span><span class="sxs-lookup"><span data-stu-id="885f6-240">Advanced scenarios</span></span>

### <a name="recursive-rendering"></a><span data-ttu-id="885f6-241">Rekursives Rendering</span><span class="sxs-lookup"><span data-stu-id="885f6-241">Recursive rendering</span></span>

<span data-ttu-id="885f6-242">Komponenten können rekursiv eingefügt werden.</span><span class="sxs-lookup"><span data-stu-id="885f6-242">Components can be nested recursively.</span></span> <span data-ttu-id="885f6-243">Dies ist nützlich, um rekursive Datenstrukturen darzustellen.</span><span class="sxs-lookup"><span data-stu-id="885f6-243">This is useful for representing recursive data structures.</span></span> <span data-ttu-id="885f6-244">Beispielsweise kann eine `TreeNode` Komponente mehr `TreeNode` Komponenten für die untergeordneten Knoten des Knotens darstellen.</span><span class="sxs-lookup"><span data-stu-id="885f6-244">For example, a `TreeNode` component can render more `TreeNode` components for each of the node's children.</span></span>

<span data-ttu-id="885f6-245">Vermeiden Sie beim rekursiv Rendering das Codieren von Mustern, die zu einer unendlichen Rekursion führen:</span><span class="sxs-lookup"><span data-stu-id="885f6-245">When rendering recursively, avoid coding patterns that result in infinite recursion:</span></span>

* <span data-ttu-id="885f6-246">Renweder eine Datenstruktur, die einen Cycle enthält, rekursiv.</span><span class="sxs-lookup"><span data-stu-id="885f6-246">Don't recursively render a data structure that contains a cycle.</span></span> <span data-ttu-id="885f6-247">Erstellen Sie beispielsweise keinen Struktur Knoten, dessen untergeordnete Elemente sich selbst enthalten.</span><span class="sxs-lookup"><span data-stu-id="885f6-247">For example, don't render a tree node whose children includes itself.</span></span>
* <span data-ttu-id="885f6-248">Erstellen Sie keine Kette von Layouts, die einen Kreis enthalten.</span><span class="sxs-lookup"><span data-stu-id="885f6-248">Don't create a chain of layouts that contain a cycle.</span></span> <span data-ttu-id="885f6-249">Erstellen Sie z. b. kein Layout, dessen Layout sich selbst ist.</span><span class="sxs-lookup"><span data-stu-id="885f6-249">For example, don't create a layout whose layout is itself.</span></span>
* <span data-ttu-id="885f6-250">Erlauben Sie einem Endbenutzer nicht, mithilfe von böswilligen Daten Einträgen oder JavaScript-Interop-aufrufen gegen revarianten der Rekursion (Regeln) zu verstoßen.</span><span class="sxs-lookup"><span data-stu-id="885f6-250">Don't allow an end user to violate recursion invariants (rules) through malicious data entry or JavaScript interop calls.</span></span>

<span data-ttu-id="885f6-251">Unendliche Schleifen beim Rendern:</span><span class="sxs-lookup"><span data-stu-id="885f6-251">Infinite loops during rendering:</span></span>

* <span data-ttu-id="885f6-252">Bewirkt, dass der Renderingprozess immer fortgesetzt wird</span><span class="sxs-lookup"><span data-stu-id="885f6-252">Causes the rendering process to continue forever.</span></span>
* <span data-ttu-id="885f6-253">Entspricht dem Erstellen einer nicht abgeschlossenen Schleife.</span><span class="sxs-lookup"><span data-stu-id="885f6-253">Is equivalent to creating an unterminated loop.</span></span>

<span data-ttu-id="885f6-254">In diesen Szenarien wird die betroffene Verbindung nicht mehr angezeigt, und der Thread versucht normalerweise Folgendes:</span><span class="sxs-lookup"><span data-stu-id="885f6-254">In these scenarios, the affected circuit hangs, and the thread usually attempts to:</span></span>

* <span data-ttu-id="885f6-255">Beanspruchen Sie beliebig viele CPU-Zeit, die das Betriebssystem unbegrenzt zulässt.</span><span class="sxs-lookup"><span data-stu-id="885f6-255">Consume as much CPU time as permitted by the operating system, indefinitely.</span></span>
* <span data-ttu-id="885f6-256">Nutzen Sie eine unbegrenzte Menge an Server Arbeitsspeicher.</span><span class="sxs-lookup"><span data-stu-id="885f6-256">Consume an unlimited amount of server memory.</span></span> <span data-ttu-id="885f6-257">Die Verwendung von unbegrenztem Arbeitsspeicher entspricht dem Szenario, in dem eine nicht abgeschlossene Schleife einer Auflistung bei jeder Iterationen Einträge hinzufügt.</span><span class="sxs-lookup"><span data-stu-id="885f6-257">Consuming unlimited memory is equivalent to the scenario where an unterminated loop adds entries to a collection on every iteration.</span></span>

<span data-ttu-id="885f6-258">Um unendliche Rekursions Muster zu vermeiden, stellen Sie sicher, dass der rekursive Renderingcode geeignete Stopp Bedingungen</span><span class="sxs-lookup"><span data-stu-id="885f6-258">To avoid infinite recursion patterns, ensure that recursive rendering code contains suitable stopping conditions.</span></span>

### <a name="custom-render-tree-logic"></a><span data-ttu-id="885f6-259">Benutzerdefinierte renderstrukturlogik</span><span class="sxs-lookup"><span data-stu-id="885f6-259">Custom render tree logic</span></span>

<span data-ttu-id="885f6-260">Die meisten blazor-Komponenten werden als *Razor* -Dateien implementiert und kompiliert, um Logik zu erzeugen, die auf einem `RenderTreeBuilder` zum Renderingergebnis funktioniert.</span><span class="sxs-lookup"><span data-stu-id="885f6-260">Most Blazor components are implemented as *.razor* files and are compiled to produce logic that operates on a `RenderTreeBuilder` to render their output.</span></span> <span data-ttu-id="885f6-261">Ein Entwickler kann `RenderTreeBuilder` Logik mithilfe von prozeduralem C# Code manuell implementieren.</span><span class="sxs-lookup"><span data-stu-id="885f6-261">A developer may manually implement `RenderTreeBuilder` logic using procedural C# code.</span></span> <span data-ttu-id="885f6-262">Weitere Informationen finden Sie unter <xref:blazor/components#manual-rendertreebuilder-logic>.</span><span class="sxs-lookup"><span data-stu-id="885f6-262">For more information, see <xref:blazor/components#manual-rendertreebuilder-logic>.</span></span>

> [!WARNING]
> <span data-ttu-id="885f6-263">Die Verwendung der Logik des manuellen renderbaum-Generators wird als erweitertes und unsichere Szenario angesehen und wird für die allgemeine Komponentenentwicklung nicht empfohlen.</span><span class="sxs-lookup"><span data-stu-id="885f6-263">Use of manual render tree builder logic is considered an advanced and unsafe scenario, not recommended for general component development.</span></span>

<span data-ttu-id="885f6-264">Wenn `RenderTreeBuilder` Code geschrieben wurde, muss der Entwickler die Richtigkeit des Codes sicherstellen.</span><span class="sxs-lookup"><span data-stu-id="885f6-264">If `RenderTreeBuilder` code is written, the developer must guarantee the correctness of the code.</span></span> <span data-ttu-id="885f6-265">Der Entwickler muss beispielsweise Folgendes sicherstellen:</span><span class="sxs-lookup"><span data-stu-id="885f6-265">For example, the developer must ensure that:</span></span>

* <span data-ttu-id="885f6-266">Aufrufe von `OpenElement` und `CloseElement` sind ordnungsgemäß ausgeglichen.</span><span class="sxs-lookup"><span data-stu-id="885f6-266">Calls to `OpenElement` and `CloseElement` are correctly balanced.</span></span>
* <span data-ttu-id="885f6-267">Attribute werden nur an den richtigen Stellen hinzugefügt.</span><span class="sxs-lookup"><span data-stu-id="885f6-267">Attributes are only added in the correct places.</span></span>

<span data-ttu-id="885f6-268">Falsche manuelle Renderstruktur-Generator-Logik kann willkürlich nicht definiertes Verhalten verursachen, z. b. Abstürze, Serverabstürze und Sicherheitsrisiken.</span><span class="sxs-lookup"><span data-stu-id="885f6-268">Incorrect manual render tree builder logic can cause arbitrary undefined behavior, including crashes, server hangs, and security vulnerabilities.</span></span>

<span data-ttu-id="885f6-269">Beachten Sie die manuelle renderbaum-Generator-Logik auf der gleichen Ebene der Komplexität und mit dem gleichen Maß an *Gefahr* wie das Schreiben von Assemblycode oder MSIL-Anweisungen per Hand.</span><span class="sxs-lookup"><span data-stu-id="885f6-269">Consider manual render tree builder logic on the same level of complexity and with the same level of *danger* as writing assembly code or MSIL instructions by hand.</span></span>
