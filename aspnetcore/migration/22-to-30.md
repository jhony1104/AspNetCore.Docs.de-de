---
title: Migrieren von ASP.NET Core 2.2 auf 3.0-Vorschau
author: tdykstra
description: Erfahren Sie, wie Sie ein ASP.NET Core-2.2-Projekt zu ASP.NET Core 3.0 zu migrieren.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: 1499239ae94919d1b094e6e55970d9920c4da2cd
ms.sourcegitcommit: 8516b586541e6ba402e57228e356639b85dfb2b9
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 07/11/2019
ms.locfileid: "67814873"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="62f2d-103">Migrieren von ASP.NET Core 2.2 auf 3.0</span><span class="sxs-lookup"><span data-stu-id="62f2d-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="62f2d-104">Durch [Scott Addie](https://github.com/scottaddie) und [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="62f2d-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="62f2d-105">In diesem Artikel wird erläutert, wie Sie ein vorhandenes ASP.NET Core-2.2-Projekt auf ASP.NET Core 3.0 zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="62f2d-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

## <a name="prerequisites"></a><span data-ttu-id="62f2d-106">Erforderliche Komponenten</span><span class="sxs-lookup"><span data-stu-id="62f2d-106">Prerequisites</span></span>

# <a name="visual-studiotabvisual-studio"></a>[<span data-ttu-id="62f2d-107">Visual Studio</span><span class="sxs-lookup"><span data-stu-id="62f2d-107">Visual Studio</span></span>](#tab/visual-studio)

[!INCLUDE[](~/includes/net-core-prereqs-vs-3.0.md)]

# <a name="visual-studio-codetabvisual-studio-code"></a>[<span data-ttu-id="62f2d-108">Visual Studio Code</span><span class="sxs-lookup"><span data-stu-id="62f2d-108">Visual Studio Code</span></span>](#tab/visual-studio-code)

[!INCLUDE[](~/includes/net-core-prereqs-vsc-3.0.md)]

# <a name="visual-studio-for-mactabvisual-studio-mac"></a>[<span data-ttu-id="62f2d-109">Visual Studio für Mac</span><span class="sxs-lookup"><span data-stu-id="62f2d-109">Visual Studio for Mac</span></span>](#tab/visual-studio-mac)

[!INCLUDE[](~/includes/net-core-prereqs-mac-3.0.md)]

---

## <a name="update-the-project-file"></a><span data-ttu-id="62f2d-110">Aktualisieren Sie die Projektdatei</span><span class="sxs-lookup"><span data-stu-id="62f2d-110">Update the project file</span></span>

* <span data-ttu-id="62f2d-111">Legen Sie die [Target frameworkMoniker (TFM),](/dotnet/standard/frameworks) zu `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-111">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="62f2d-112">Entfernen Sie alle `<PackageReference>` auf die ["Microsoft.aspnetcore.All"](xref:fundamentals/metapackage) oder [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapaket.</span><span class="sxs-lookup"><span data-stu-id="62f2d-112">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="62f2d-113">Update der `Version` Attribut für die verbleibenden `<PackageReference>` Elemente für `Microsoft.AspNetCore.*` Pakete für die Vorschau (z. B. `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="62f2d-113">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="62f2d-114">Wenn keine 3.0 Version eines Pakets vorhanden ist, das Paket möglicherweise 3.0 veraltet sind.</span><span class="sxs-lookup"><span data-stu-id="62f2d-114">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="62f2d-115">Viele dieser Pakete sind Teil des `Microsoft.AspNetCore.App` und sollte nicht einzeln referenziert werden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-115">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="62f2d-116">Eine vorläufige Liste von Paketen, die nicht mehr in 3.0 erstellt, finden Sie unter [nicht mehr von Paketen für das freigegebene Framework-Assemblys in 3.0 (Aspnet/AspNetCore-3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="62f2d-116">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="62f2d-117">Die *gemeinsam genutzten Frameworks* ist ein Satz von Assemblys (*DLL* Dateien), die auf dem Computer installiert und auf die verwiesen wird durch `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-117">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="62f2d-118">Weitere Informationen finden Sie unter [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) (Das freigegebene Framework).</span><span class="sxs-lookup"><span data-stu-id="62f2d-118">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="62f2d-119">Die Assemblys für mehrere wichtige Komponenten wurden entfernt von `Microsoft.AspNetCore.App` 3.0.</span><span class="sxs-lookup"><span data-stu-id="62f2d-119">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="62f2d-120">Hinzufügen `<PackageReference>` Elemente, die bei Verwendung von APIs von den in aufgeführten Pakete [Assemblys aus Microsoft.AspNetCore.App 3.0 entfernt (Aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="62f2d-120">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="62f2d-121">Entfernt Komponenten gehören:</span><span class="sxs-lookup"><span data-stu-id="62f2d-121">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="62f2d-122">Die Liste der Protokollversand in Assemblys `Microsoft.AspNetCore.App` noch nicht abgeschlossen wurde und bevor 3.0 RTM-Version ändert.</span><span class="sxs-lookup"><span data-stu-id="62f2d-122">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="62f2d-123">Betrachten Sie folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="62f2d-123">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="62f2d-124">Die `ReadAsAsync` im obigen Code aufgerufene Methode befindet sich im `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-124">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="62f2d-125">Installieren Sie die [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet-Paket zum Beheben des Problems Kompilierung 3.0.</span><span class="sxs-lookup"><span data-stu-id="62f2d-125">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="62f2d-126">Hinzufügen [Json.NET Unterstützung](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="62f2d-126">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="62f2d-127">Projekte standardmäßig die [: prozessinternes Hostingmodell](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 oder höher.</span><span class="sxs-lookup"><span data-stu-id="62f2d-127">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="62f2d-128">Sie können optional auch entfernen, die `<AspNetCoreHostingModel>` Eigenschaft in der Projektdatei, wenn der Wert `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-128">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="62f2d-129">Json.NET-Unterstützung</span><span class="sxs-lookup"><span data-stu-id="62f2d-129">Json.NET support</span></span>

<span data-ttu-id="62f2d-130">Als Teil der Arbeit [verbessern von ASP.NET Core gemeinsam genutzten Frameworks](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) aus ASP.NET Core gemeinsam genutzten Frameworks entfernt wurde.</span><span class="sxs-lookup"><span data-stu-id="62f2d-130">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="62f2d-131">So verwenden Sie Json.NET in einem ASP.NET Core 3.0-Projekt:</span><span class="sxs-lookup"><span data-stu-id="62f2d-131">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="62f2d-132">Fügen Sie einen Paketverweis auf [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="62f2d-132">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="62f2d-133">Update `Startup.ConfigureServices` aufzurufende `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-133">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```
  
  <span data-ttu-id="62f2d-134">`AddNewtonsoftJson` ist kompatibel mit der neuen MVC-Dienst-Registrierungsmethoden:</span><span class="sxs-lookup"><span data-stu-id="62f2d-134">`AddNewtonsoftJson` is compatible with the new MVC service registration methods:</span></span>

  * `AddRazorPages`
  * `AddControllersWithViews`
  * `AddControllers`

  ```csharp
  services.AddControllers()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="62f2d-135">Json.NET-Einstellungen können festgelegt werden, in dem Aufruf von `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-135">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="62f2d-136">MVC-Service-Registrierung</span><span class="sxs-lookup"><span data-stu-id="62f2d-136">MVC service registration</span></span>

<span data-ttu-id="62f2d-137">ASP.NET Core 3.0 fügt neue Optionen für die Registrierung von MVC-Szenarien in `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-137">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="62f2d-138">Drei neue Methoden der Erweiterung der obersten Ebene im Zusammenhang mit MVC-Szenarien auf `IServiceCollection` stehen zur Verfügung.</span><span class="sxs-lookup"><span data-stu-id="62f2d-138">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="62f2d-139">Vorlagen verwenden diese neuen Methoden anstelle von `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-139">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="62f2d-140">Allerdings `AddMvc` weiterhin verhält sich wie in früheren Versionen hat.</span><span class="sxs-lookup"><span data-stu-id="62f2d-140">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="62f2d-141">Das folgende Beispiel fügt Unterstützung für Controller und API-Funktionen, jedoch keine Sichten oder Seiten.</span><span class="sxs-lookup"><span data-stu-id="62f2d-141">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="62f2d-142">Dieser Code wird die API-Vorlage verwendet:</span><span class="sxs-lookup"><span data-stu-id="62f2d-142">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="62f2d-143">Das folgende Beispiel fügt Unterstützung für Controller, API-Funktionen und Ansichten, aber nicht in Seiten.</span><span class="sxs-lookup"><span data-stu-id="62f2d-143">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="62f2d-144">Dieser Code wird die Webanwendung (MVC)-Vorlage verwendet:</span><span class="sxs-lookup"><span data-stu-id="62f2d-144">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="62f2d-145">Das folgende Beispiel fügt Unterstützung für Razor-Seiten und minimale Controller.</span><span class="sxs-lookup"><span data-stu-id="62f2d-145">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="62f2d-146">Die Vorlage "Webanwendung" verwendet dieser Code:</span><span class="sxs-lookup"><span data-stu-id="62f2d-146">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="62f2d-147">Die neuen Methoden können auch kombiniert werden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-147">The new methods can also be combined.</span></span> <span data-ttu-id="62f2d-148">Das folgende Beispiel entspricht dem Aufruf `AddMvc` in ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="62f2d-148">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="62f2d-149">Aktualisieren der routing-Startcode</span><span class="sxs-lookup"><span data-stu-id="62f2d-149">Update routing startup code</span></span>

<span data-ttu-id="62f2d-150">Wenn eine app aufruft `UseMvc` oder `UseSignalR`, migrieren Sie die app [Endpunkt Routing](xref:fundamentals/routing) Wenn möglich.</span><span class="sxs-lookup"><span data-stu-id="62f2d-150">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="62f2d-151">Um die Kompatibilität mit früheren Versionen von MVC-Routing-Endpunkt zu verbessern, haben wir einige der Änderungen in URL-Generierung in ASP.NET Core 2.2 eingeführt wurden rückgängig gemacht.</span><span class="sxs-lookup"><span data-stu-id="62f2d-151">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="62f2d-152">Wenn Sie Probleme bei der Verwendung von Endpunkt-Routing in 2.2 aufgetreten ist, erwarten Sie Verbesserungen in ASP.NET Core 3.0, mit den folgenden Ausnahmen:</span><span class="sxs-lookup"><span data-stu-id="62f2d-152">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="62f2d-153">Wenn die app implementiert `IRouter` oder erbt von `Route`, empfiehlt es sich um zu vermeiden, Migrieren zu diesem Zeitpunkt.</span><span class="sxs-lookup"><span data-stu-id="62f2d-153">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="62f2d-154">Ihr Feedback an [Planen der Migration von Implementierungen der IRouter basierend auf den Endpunkt Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="62f2d-154">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="62f2d-155">Wenn die app greift direkt auf `RouteData.Routers` in MVC, Sie möchten zu vermeiden, Migrieren zu diesem Zeitpunkt.</span><span class="sxs-lookup"><span data-stu-id="62f2d-155">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="62f2d-156">Ihr Feedback an [migrationsanleitung für die Verwendung von RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="62f2d-156">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="62f2d-157">Endpunkt Routing unterstützt, die dieselbe Route Mustersyntax und die Routenmuster, die Erstellung von Features wie `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-157">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="62f2d-158">Endpunkt Routing unterstützt `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-158">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="62f2d-159">Attributrouting unterstützt das Endpunkt `[Route]`, `[HttpGet]`, und die anderen MVC-routing-Attribute.</span><span class="sxs-lookup"><span data-stu-id="62f2d-159">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="62f2d-160">Für die meisten Anwendungen nur `Startup` sind Änderungen erforderlich.</span><span class="sxs-lookup"><span data-stu-id="62f2d-160">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="62f2d-161">Migrieren von Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="62f2d-161">Migrate Startup.Configure</span></span>

<span data-ttu-id="62f2d-162">Ein allgemeiner Ratschlag:</span><span class="sxs-lookup"><span data-stu-id="62f2d-162">General advice:</span></span>

* <span data-ttu-id="62f2d-163">Hinzufügen `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-163">Add `UseRouting`.</span></span> 
* <span data-ttu-id="62f2d-164">Wenn die app ruft `UseStaticFiles`, platzieren Sie `UseStaticFiles` **vor** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-164">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="62f2d-165">Wenn die app-Authentifizierung/-Autorisierung Funktionen, z. B. verwendet `AuthorizePage` oder `[Authorize]`, platzieren Sie den Aufruf von `UseAuthentication` und `UseAuthorization` **nach** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-165">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="62f2d-166">Wenn die app verwendet [CORS](xref:security/cors) Funktionen, z. B. `[EnableCors]`, platzieren Sie `UseCors` weiter.</span><span class="sxs-lookup"><span data-stu-id="62f2d-166">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="62f2d-167">Ersetzen Sie dies `UseMvc` oder `UseSignalR` mit `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-167">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="62f2d-168">Folgendes ist ein Beispiel für `Startup.Configure` in einer typischen ASP.NET Core-2.2-app:</span><span class="sxs-lookup"><span data-stu-id="62f2d-168">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="62f2d-169">Nach der Aktualisierung der vorherigen `Startup.Configure` Code:</span><span class="sxs-lookup"><span data-stu-id="62f2d-169">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="62f2d-170">Middleware-Sicherheitsleitfaden</span><span class="sxs-lookup"><span data-stu-id="62f2d-170">Security middleware guidance</span></span>

<span data-ttu-id="62f2d-171">Unterstützung für die Autorisierung und CORS wird vereinheitlicht, um die [Middleware](xref:fundamentals/middleware/index) Ansatz.</span><span class="sxs-lookup"><span data-stu-id="62f2d-171">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="62f2d-172">Dies ermöglicht die Verwendung derselben Middleware und Funktionen, in diesen Szenarios.</span><span class="sxs-lookup"><span data-stu-id="62f2d-172">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="62f2d-173">Eine aktualisierte autorisierungsmiddleware wird in dieser Version bereitgestellt, und CORS-Middleware wird verbessert, damit sie die Attribute, die von MVC-Controller verwendet verstehen kann.</span><span class="sxs-lookup"><span data-stu-id="62f2d-173">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="62f2d-174">CORS</span><span class="sxs-lookup"><span data-stu-id="62f2d-174">CORS</span></span>

<span data-ttu-id="62f2d-175">Zuvor konnte CORS konfigurieren schwierig.</span><span class="sxs-lookup"><span data-stu-id="62f2d-175">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="62f2d-176">Middleware wurde angegeben, für die Verwendung in einigen Fällen, jedoch MVC-Filter für die Verwendung vorgesehen waren **ohne** die Middleware in anderen Anwendungsfälle.</span><span class="sxs-lookup"><span data-stu-id="62f2d-176">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="62f2d-177">Mit ASP.NET Core 3.0 empfehlen wir, dass alle apps, die CORS erfordern die CORS-Middleware zusammen mit dem Endpunkt Routing verwenden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-177">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="62f2d-178">`UseCors` eine Standardrichtlinie bereitgestellt werden können und `[EnableCors]` und `[DisableCors]` Attribute können verwendet werden, um die Standardrichtlinie überschreiben, sofern erforderlich.</span><span class="sxs-lookup"><span data-stu-id="62f2d-178">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="62f2d-179">Im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="62f2d-179">In the following example:</span></span>

* <span data-ttu-id="62f2d-180">CORS aktiviert ist, für alle Endpunkte mit dem `default` mit dem Namen Gruppenrichtlinie.</span><span class="sxs-lookup"><span data-stu-id="62f2d-180">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="62f2d-181">Die `MyController` -Klasse deaktiviert CORS mithilfe der `[DisableCors]` Attribut.</span><span class="sxs-lookup"><span data-stu-id="62f2d-181">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="62f2d-182">Autorisierung</span><span class="sxs-lookup"><span data-stu-id="62f2d-182">Authorization</span></span>

<span data-ttu-id="62f2d-183">In früheren Versionen von ASP.NET Core-Autorisierung-Unterstützung wurde angegeben, über die `[Authorize]` Attribut.</span><span class="sxs-lookup"><span data-stu-id="62f2d-183">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="62f2d-184">Autorisierungsmiddleware war nicht verfügbar.</span><span class="sxs-lookup"><span data-stu-id="62f2d-184">Authorization middleware wasn't available.</span></span> <span data-ttu-id="62f2d-185">In ASP.NET Core 3.0 ist die autorisierungsmiddleware erforderlich.</span><span class="sxs-lookup"><span data-stu-id="62f2d-185">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="62f2d-186">Es wird empfohlen, platzieren die Autorisierungsmiddleware für ASP.NET Core (`UseAuthorization`) unmittelbar nach `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-186">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="62f2d-187">Die Autorisierungsmiddleware kann auch mit einer Standardrichtlinie konfiguriert werden, die überschrieben werden kann.</span><span class="sxs-lookup"><span data-stu-id="62f2d-187">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="62f2d-188">In ASP.NET Core 3.0 oder höher `UseAuthorization` unter "lautet" `Startup.Configure`, und die folgenden `HomeController` erfordert eine signierte Benutzer:</span><span class="sxs-lookup"><span data-stu-id="62f2d-188">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="62f2d-189">Wenn die app verwendet eine `AuthorizeFilter` als globalen Filter in MVC, es wird empfohlen den Code zum Bereitstellen einer Richtlinie für die Umgestaltung der `UseAuthorization` Middleware.</span><span class="sxs-lookup"><span data-stu-id="62f2d-189">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="62f2d-190">Im folgenden Beispiel eine benutzerdefinierte Richtlinie auf alle angewendet werden angefordert, wenn `UseAuthorization` aufgerufen wird, und die `HomeController` ermöglicht den Zugriff, ohne dass der Benutzer das Anmelden bei der app:</span><span class="sxs-lookup"><span data-stu-id="62f2d-190">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="62f2d-191">Von Middleware-Autorisierung funktioniert, ohne das Framework, dass bestimmte Kenntnisse der Autorisierung.</span><span class="sxs-lookup"><span data-stu-id="62f2d-191">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="62f2d-192">Z. B. [integritätsprüfungen](xref:host-and-deploy/health-checks) enthält keine spezifischen Wissensquellen für die Autorisierung, aber integritätsprüfungen können eine konfigurierbare Autorisierungsrichtlinie, die von der Middleware angewendet.</span><span class="sxs-lookup"><span data-stu-id="62f2d-192">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="62f2d-193">Im folgenden Beispiel `UseAuthorization` Autorisierung ohne eine Standardrichtlinie, verarbeitet aber die `/healthz` integritätsüberprüfung muss der Benutzer in der `admin` Rolle:</span><span class="sxs-lookup"><span data-stu-id="62f2d-193">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="62f2d-194">Schutz wird für einige Szenarien implementiert werden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-194">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="62f2d-195">`UseEndpoint` Middleware wird eine Ausnahme ausgelöst, wenn eine Autorisierung oder CORS-Richtlinie aufgrund fehlender Middleware übersprungen wird.</span><span class="sxs-lookup"><span data-stu-id="62f2d-195">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="62f2d-196">Unterstützung der Analyzer angeben, dass zusätzliches Feedback zu einer fehlerhaften Konfiguration ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="62f2d-196">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="62f2d-197">Migrieren von SignalR</span><span class="sxs-lookup"><span data-stu-id="62f2d-197">Migrate SignalR</span></span>

<span data-ttu-id="62f2d-198">Zuordnen von SignalR-Hubs jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-198">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="62f2d-199">Ordnen Sie jeden Hub mit `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-199">Map each hub with `MapHub`.</span></span> <span data-ttu-id="62f2d-200">Jeder Hub ist explizit aufgelistet, wie in früheren Versionen.</span><span class="sxs-lookup"><span data-stu-id="62f2d-200">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="62f2d-201">Im folgenden Beispiel, Unterstützung für die `ChatHub` SignalR-Hub hinzugefügt wird:</span><span class="sxs-lookup"><span data-stu-id="62f2d-201">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="62f2d-202">Migrieren von MVC-Controller</span><span class="sxs-lookup"><span data-stu-id="62f2d-202">Migrate MVC controllers</span></span>

<span data-ttu-id="62f2d-203">Zuordnung der Controller jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-203">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="62f2d-204">Hinzufügen `MapControllers` Wenn die app das attributrouting verwendet.</span><span class="sxs-lookup"><span data-stu-id="62f2d-204">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="62f2d-205">Da die routing-Unterstützung für viele Frameworks in ASP.NET Core 3.0 oder höher enthält, ist das Hinzufügen von Attribut-Routing-Controller teilnehmen.</span><span class="sxs-lookup"><span data-stu-id="62f2d-205">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="62f2d-206">Ersetzen Sie die folgenden:</span><span class="sxs-lookup"><span data-stu-id="62f2d-206">Replace the following:</span></span>

* <span data-ttu-id="62f2d-207">`MapRoute` mit `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="62f2d-207">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="62f2d-208">`MapAreaRoute` mit `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="62f2d-208">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="62f2d-209">Da routing jetzt Unterstützung für mehr als nur MVC enthält, wurde die Terminologie geändert, um stellen diese Methoden, die eindeutig angeben, was sie tun.</span><span class="sxs-lookup"><span data-stu-id="62f2d-209">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="62f2d-210">Herkömmliche Routen wie z. B. `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` gelten in der Reihenfolge, die sie hinzugefügt werden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-210">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="62f2d-211">Platzieren Sie zuerst die spezifischere Routen (z. B. die Routen für einen Bereich).</span><span class="sxs-lookup"><span data-stu-id="62f2d-211">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="62f2d-212">Im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="62f2d-212">In the following example:</span></span>

* <span data-ttu-id="62f2d-213">`MapControllers` Fügt Unterstützung für die Attribut-Routing-Controller hinzu.</span><span class="sxs-lookup"><span data-stu-id="62f2d-213">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="62f2d-214">`MapAreaControllerRoute` Fügt eine konventionelle Route für Controller in einem Bereich hinzu.</span><span class="sxs-lookup"><span data-stu-id="62f2d-214">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="62f2d-215">`MapControllerRoute` Fügt eine konventionelle Route für Controller.</span><span class="sxs-lookup"><span data-stu-id="62f2d-215">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="62f2d-216">Migrieren von Razor-Seiten</span><span class="sxs-lookup"><span data-stu-id="62f2d-216">Migrate Razor Pages</span></span>

<span data-ttu-id="62f2d-217">Zuordnen von Razor-Seiten jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-217">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="62f2d-218">Hinzufügen `MapRazorPages` Wenn die app mit Razor-Seiten verwendet.</span><span class="sxs-lookup"><span data-stu-id="62f2d-218">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="62f2d-219">Da der Endpunkt-Routing-Unterstützung für viele Frameworks enthält, ist das Hinzufügen von Razor-Seiten jetzt teilnehmen.</span><span class="sxs-lookup"><span data-stu-id="62f2d-219">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="62f2d-220">Im folgenden Beispiel `MapRazorPages` fügt Unterstützung für Razor-Seiten hinzu:</span><span class="sxs-lookup"><span data-stu-id="62f2d-220">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="62f2d-221">Verwenden Sie MVC ohne Endpunkt weiterzuleiten.</span><span class="sxs-lookup"><span data-stu-id="62f2d-221">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="62f2d-222">Mit MVC über `UseMvc` oder `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 erfordert eine explizite opt-in in `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-222">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="62f2d-223">Dies ist erforderlich, da MVC wissen muss, ob sie auf die Autorisierungs- und CORS-Middleware, während der Initialisierung verlassen kann.</span><span class="sxs-lookup"><span data-stu-id="62f2d-223">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="62f2d-224">Ein Analyzer wird bereitgestellt, die warnt, wenn die app versucht, eine nicht unterstützte Konfiguration zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="62f2d-224">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="62f2d-225">Wenn die app ältere "requires" `IRouter` unterstützen, deaktivieren Sie `EnableEndpointRouting` mithilfe einer der folgenden Ansätze `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-225">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="62f2d-226">Migrieren von integritätsprüfungen</span><span class="sxs-lookup"><span data-stu-id="62f2d-226">Migrate health checks</span></span>

<span data-ttu-id="62f2d-227">Integritätsprüfungen können verwendet werden, als eine *Router Adware* mit Routing-Endpunkt.</span><span class="sxs-lookup"><span data-stu-id="62f2d-227">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="62f2d-228">Hinzufügen `MapHealthChecks` Routing-Endpunkt mit der Integrität überprüft.</span><span class="sxs-lookup"><span data-stu-id="62f2d-228">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="62f2d-229">Die `MapHealthChecks` Methode akzeptiert Argumente, die ähnlich wie `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-229">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="62f2d-230">Der Vorteil der Verwendung `MapHealthChecks` über `UseHealthChecks` ist die Fähigkeit, Autorisierung anzuwenden und höher eine präzisere Kontrolle über die entsprechende Richtlinie haben.</span><span class="sxs-lookup"><span data-stu-id="62f2d-230">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="62f2d-231">Im folgenden Beispiel `MapHealthChecks` wird aufgerufen, für einen Endpunkt auf Kontrollkästchen `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-231">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="62f2d-232">HostBuilder ersetzt WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="62f2d-232">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="62f2d-233">Verwenden Sie die Vorlagen für ASP.NET Core 3.0 [generischer Host](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="62f2d-233">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="62f2d-234">Frühere Versionen verwendeten [Webhost](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="62f2d-234">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="62f2d-235">Der folgende Code zeigt die generierte Vorlage für ASP.NET Core 3.0 `Program` Klasse:</span><span class="sxs-lookup"><span data-stu-id="62f2d-235">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="62f2d-236">Der folgende Code zeigt die ASP.NET Core-2.2 Vorlage generierte `Program` Klasse:</span><span class="sxs-lookup"><span data-stu-id="62f2d-236">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="62f2d-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> verbleibt im 3.0, und der Typ des der `webBuilder` im obigen Codebeispiel angezeigt.</span><span class="sxs-lookup"><span data-stu-id="62f2d-237"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="62f2d-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> wird in einer zukünftigen Version als veraltet markiert und ersetzt Sie durch `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="62f2d-238"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="62f2d-239">Die wichtigste Neuerung von `WebHostBuilder` zu `HostBuilder` befindet sich im [Abhängigkeitsinjektion (Dependency Injection)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="62f2d-239">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="62f2d-240">Bei Verwendung `HostBuilder`, Sie können nur Folgendes einfügen: <xref:Microsoft.Extensions.Configuration.IConfiguration> und <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> in `Startup`Konstruktor.</span><span class="sxs-lookup"><span data-stu-id="62f2d-240">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="62f2d-241">Die `HostBuilder` DI Einschränkungen:</span><span class="sxs-lookup"><span data-stu-id="62f2d-241">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="62f2d-242">Aktivieren Sie den DI-Container nur ein Mal erstellt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="62f2d-242">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="62f2d-243">Vermeiden die resultierenden Probleme mit der Objektlebensdauer wie das Auflösen von mehrere Instanzen des Singletons.</span><span class="sxs-lookup"><span data-stu-id="62f2d-243">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="62f2d-244">Aktualisieren Sie Code, SignalR</span><span class="sxs-lookup"><span data-stu-id="62f2d-244">Update SignalR code</span></span>

<span data-ttu-id="62f2d-245">`System.Text.Json` ist nun standardmäßig vom Client und Server, der Hub-Protokoll verwendet.</span><span class="sxs-lookup"><span data-stu-id="62f2d-245">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="62f2d-246">In `Startup.ConfigureServices`, rufen Sie `AddJsonProtocol` serialisiereroptionen festlegen.</span><span class="sxs-lookup"><span data-stu-id="62f2d-246">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="62f2d-247">**Server:**</span><span class="sxs-lookup"><span data-stu-id="62f2d-247">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="62f2d-248">**Client:**</span><span class="sxs-lookup"><span data-stu-id="62f2d-248">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="62f2d-249">Wechseln Sie zu "newtonsoft.JSON"</span><span class="sxs-lookup"><span data-stu-id="62f2d-249">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="62f2d-250">Bei Verwendung von Features von `Newtonsoft.Json` , die nicht unterstützt `System.Text.Json`, können Sie zum wechseln `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-250">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="62f2d-251">Installieren Sie die [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet-Paket.</span><span class="sxs-lookup"><span data-stu-id="62f2d-251">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="62f2d-252">Verketten von auf dem Client eine `AddNewtonsoftJsonProtocol` Methodenaufruf der `HubConnectionBuilder` Instanz:</span><span class="sxs-lookup"><span data-stu-id="62f2d-252">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="62f2d-253">Verketten von auf dem Server ein `AddNewtonsoftJsonProtocol` Methodenaufruf der `AddSignalR` -Methodenaufruf in `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="62f2d-253">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="62f2d-254">Abonnieren Sie Runtime-Kompilierung</span><span class="sxs-lookup"><span data-stu-id="62f2d-254">Opt in to runtime compilation</span></span>

<span data-ttu-id="62f2d-255">In 3.0 ist die Runtime-Kompilierung ein opt-in-Szenario.</span><span class="sxs-lookup"><span data-stu-id="62f2d-255">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="62f2d-256">Um die Runtime-Kompilierung zu aktivieren, finden Sie unter <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="62f2d-256">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
