---
title: Migrieren von ASP.NET Core 2.2 auf 3.0-Vorschau
author: tdykstra
description: Erfahren Sie, wie Sie ein ASP.NET Core-2.2-Projekt zu ASP.NET Core 3.0 zu migrieren.
ms.author: tdykstra
ms.custom: mvc
ms.date: 06/17/2019
uid: migration/22-to-30
ms.openlocfilehash: abd31fa8ed27af230f347608f3e60f855a46b908
ms.sourcegitcommit: 28a2874765cefe9eaa068dceb989a978ba2096aa
ms.translationtype: MT
ms.contentlocale: de-DE
ms.lasthandoff: 06/17/2019
ms.locfileid: "67167120"
---
# <a name="migrate-from-aspnet-core-22-to-30"></a><span data-ttu-id="b181b-103">Migrieren von ASP.NET Core 2.2 auf 3.0</span><span class="sxs-lookup"><span data-stu-id="b181b-103">Migrate from ASP.NET Core 2.2 to 3.0</span></span>

<span data-ttu-id="b181b-104">Durch [Scott Addie](https://github.com/scottaddie) und [Rick Anderson](https://twitter.com/RickAndMSFT)</span><span class="sxs-lookup"><span data-stu-id="b181b-104">By [Scott Addie](https://github.com/scottaddie) and [Rick Anderson](https://twitter.com/RickAndMSFT)</span></span>

<span data-ttu-id="b181b-105">In diesem Artikel wird erläutert, wie Sie ein vorhandenes ASP.NET Core-2.2-Projekt auf ASP.NET Core 3.0 zu aktualisieren.</span><span class="sxs-lookup"><span data-stu-id="b181b-105">This article explains how to update an existing ASP.NET Core 2.2 project to ASP.NET Core 3.0.</span></span>

[!INCLUDE[](~/includes/net-core-prereqs-all-3.0.md)]

## <a name="update-the-project-file"></a><span data-ttu-id="b181b-106">Aktualisieren Sie die Projektdatei</span><span class="sxs-lookup"><span data-stu-id="b181b-106">Update the project file</span></span>

* <span data-ttu-id="b181b-107">Legen Sie die [Target frameworkMoniker (TFM),](/dotnet/standard/frameworks#referring-to-frameworks) zu `netcoreapp3.0`:</span><span class="sxs-lookup"><span data-stu-id="b181b-107">Set the [Target Framework Moniker (TFM)](/dotnet/standard/frameworks#referring-to-frameworks) to `netcoreapp3.0`:</span></span>

  ```xml
  <TargetFramework>netcoreapp3.0</TargetFramework>
  ```

* <span data-ttu-id="b181b-108">Entfernen Sie alle `<PackageReference>` auf die ["Microsoft.aspnetcore.All"](xref:fundamentals/metapackage) oder [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapaket.</span><span class="sxs-lookup"><span data-stu-id="b181b-108">Remove any `<PackageReference>` to the [Microsoft.AspNetCore.All](xref:fundamentals/metapackage) or [Microsoft.AspNetCore.App](xref:fundamentals/metapackage-app) metapackage.</span></span>

* <span data-ttu-id="b181b-109">Update der `Version` Attribut für die verbleibenden `<PackageReference>` Elemente für `Microsoft.AspNetCore.*` Pakete für die Vorschau (z. B. `3.0.0-preview5-19227-01`).</span><span class="sxs-lookup"><span data-stu-id="b181b-109">Update the `Version` attribute on remaining `<PackageReference>` elements for `Microsoft.AspNetCore.*` packages to the current preview (for example, `3.0.0-preview5-19227-01`).</span></span>

  <span data-ttu-id="b181b-110">Wenn keine 3.0 Version eines Pakets vorhanden ist, das Paket möglicherweise 3.0 veraltet sind.</span><span class="sxs-lookup"><span data-stu-id="b181b-110">If there's no 3.0 version of a package, the package might have been deprecated in 3.0.</span></span> <span data-ttu-id="b181b-111">Viele dieser Pakete sind Teil des `Microsoft.AspNetCore.App` und sollte nicht einzeln referenziert werden.</span><span class="sxs-lookup"><span data-stu-id="b181b-111">Many of these packages are part of `Microsoft.AspNetCore.App` and shouldn't be referenced individually.</span></span> <span data-ttu-id="b181b-112">Eine vorläufige Liste von Paketen, die nicht mehr in 3.0 erstellt, finden Sie unter [nicht mehr von Paketen für das freigegebene Framework-Assemblys in 3.0 (Aspnet/AspNetCore-3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span><span class="sxs-lookup"><span data-stu-id="b181b-112">For a preliminary list of packages no longer produced in 3.0, see [Stop producing packages for shared framework assemblies in 3.0 (aspnet/AspNetCore #3756)](https://github.com/aspnet/AspNetCore/issues/3756).</span></span> <span data-ttu-id="b181b-113">Die *gemeinsam genutzten Frameworks* ist ein Satz von Assemblys (*DLL* Dateien), die auf dem Computer installiert und auf die verwiesen wird durch `Microsoft.AspNetCore.App`.</span><span class="sxs-lookup"><span data-stu-id="b181b-113">The *shared framework* is the set of assemblies (*.dll* files) that are installed on the machine and referenced by `Microsoft.AspNetCore.App`.</span></span> <span data-ttu-id="b181b-114">Weitere Informationen finden Sie unter [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/) (Das freigegebene Framework).</span><span class="sxs-lookup"><span data-stu-id="b181b-114">For more information, see [The shared framework](https://natemcmaster.com/blog/2018/08/29/netcore-primitives-2/).</span></span>

* <span data-ttu-id="b181b-115">Die Assemblys für mehrere wichtige Komponenten wurden entfernt von `Microsoft.AspNetCore.App` 3.0.</span><span class="sxs-lookup"><span data-stu-id="b181b-115">The assemblies for several notable components were removed from `Microsoft.AspNetCore.App` in 3.0.</span></span> <span data-ttu-id="b181b-116">Hinzufügen `<PackageReference>` Elemente, die bei Verwendung von APIs von den in aufgeführten Pakete [Assemblys aus Microsoft.AspNetCore.App 3.0 entfernt (Aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span><span class="sxs-lookup"><span data-stu-id="b181b-116">Add `<PackageReference>` elements if you're using APIs from packages listed in [Assemblies being removed from Microsoft.AspNetCore.App 3.0 (aspnet/AspNetCore #3755)](https://github.com/aspnet/AspNetCore/issues/3755).</span></span>

  <span data-ttu-id="b181b-117">Entfernt Komponenten gehören:</span><span class="sxs-lookup"><span data-stu-id="b181b-117">Examples of removed components include:</span></span>

  * `Microsoft.AspNet.WebApi.Client`
  * `Microsoft.EntityFrameworkCore`
  * `System.Data.SqlClient`

  <span data-ttu-id="b181b-118">Die Liste der Protokollversand in Assemblys `Microsoft.AspNetCore.App` noch nicht abgeschlossen wurde und bevor 3.0 RTM-Version ändert.</span><span class="sxs-lookup"><span data-stu-id="b181b-118">The list of assemblies shipping in `Microsoft.AspNetCore.App` hasn't been finalized and will change before 3.0 RTM.</span></span>

  <span data-ttu-id="b181b-119">Betrachten Sie folgenden Code:</span><span class="sxs-lookup"><span data-stu-id="b181b-119">Consider the following code:</span></span>

  ```csharp
  var branches = await response.Content.ReadAsAsync<IEnumerable<GitHubBranch>>();
  ```

  <span data-ttu-id="b181b-120">Die `ReadAsAsync` im obigen Code aufgerufene Methode befindet sich im `Microsoft.AspNet.WebApi.Client`.</span><span class="sxs-lookup"><span data-stu-id="b181b-120">The `ReadAsAsync` method called in the preceding code is included in `Microsoft.AspNet.WebApi.Client`.</span></span> <span data-ttu-id="b181b-121">Installieren Sie die [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet-Paket zum Beheben des Problems Kompilierung 3.0.</span><span class="sxs-lookup"><span data-stu-id="b181b-121">Install the [Microsoft.AspNet.WebApi.Client](https://www.nuget.org/packages/Microsoft.AspNet.WebApi.Client/) NuGet package to resolve the compilation issue in 3.0.</span></span>

* <span data-ttu-id="b181b-122">Hinzufügen [Json.NET Unterstützung](#jsonnet-support).</span><span class="sxs-lookup"><span data-stu-id="b181b-122">Add [Json.NET support](#jsonnet-support).</span></span>

* <span data-ttu-id="b181b-123">Projekte standardmäßig die [: prozessinternes Hostingmodell](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 oder höher.</span><span class="sxs-lookup"><span data-stu-id="b181b-123">Projects default to the [in-process hosting model](xref:host-and-deploy/aspnet-core-module#in-process-hosting-model) in ASP.NET Core 3.0 or later.</span></span> <span data-ttu-id="b181b-124">Sie können optional auch entfernen, die `<AspNetCoreHostingModel>` Eigenschaft in der Projektdatei, wenn der Wert `InProcess`.</span><span class="sxs-lookup"><span data-stu-id="b181b-124">You may optionally remove the `<AspNetCoreHostingModel>` property in the project file if its value is `InProcess`.</span></span>

## <a name="jsonnet-support"></a><span data-ttu-id="b181b-125">Json.NET-Unterstützung</span><span class="sxs-lookup"><span data-stu-id="b181b-125">Json.NET support</span></span>

<span data-ttu-id="b181b-126">Als Teil der Arbeit [verbessern von ASP.NET Core gemeinsam genutzten Frameworks](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) aus ASP.NET Core gemeinsam genutzten Frameworks entfernt wurde.</span><span class="sxs-lookup"><span data-stu-id="b181b-126">As part of the work to [improve the ASP.NET Core shared framework](https://blogs.msdn.microsoft.com/webdev/2018/10/29/a-first-look-at-changes-coming-in-asp-net-core-3-0/), [Json.NET](https://www.newtonsoft.com/json/help/html/Introduction.htm) has been removed from the ASP.NET Core shared framework.</span></span>

<span data-ttu-id="b181b-127">So verwenden Sie Json.NET in einem ASP.NET Core 3.0-Projekt:</span><span class="sxs-lookup"><span data-stu-id="b181b-127">To use Json.NET in an ASP.NET Core 3.0 project:</span></span>

* <span data-ttu-id="b181b-128">Fügen Sie einen Paketverweis auf [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span><span class="sxs-lookup"><span data-stu-id="b181b-128">Add a package reference to [Microsoft.AspNetCore.Mvc.NewtonsoftJson](https://nuget.org/packages/Microsoft.AspNetCore.Mvc.NewtonsoftJson).</span></span>
* <span data-ttu-id="b181b-129">Update `Startup.ConfigureServices` aufzurufende `AddNewtonsoftJson`.</span><span class="sxs-lookup"><span data-stu-id="b181b-129">Update `Startup.ConfigureServices` to call `AddNewtonsoftJson`.</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson();
  ```

  <span data-ttu-id="b181b-130">Json.NET-Einstellungen können festgelegt werden, in dem Aufruf von `AddNewtonsoftJson`:</span><span class="sxs-lookup"><span data-stu-id="b181b-130">Json.NET settings can be set in the call to `AddNewtonsoftJson`:</span></span>

  ```csharp
  services.AddMvc()
      .AddNewtonsoftJson(options =>
             options.SerializerSettings.ContractResolver =
                new CamelCasePropertyNamesContractResolver());
  ```

## <a name="mvc-service-registration"></a><span data-ttu-id="b181b-131">MVC-Service-Registrierung</span><span class="sxs-lookup"><span data-stu-id="b181b-131">MVC service registration</span></span>

<span data-ttu-id="b181b-132">ASP.NET Core 3.0 fügt neue Optionen für die Registrierung von MVC-Szenarien in `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="b181b-132">ASP.NET Core 3.0 adds new options for registering MVC scenarios inside `Startup.ConfigureServices`.</span></span>

<span data-ttu-id="b181b-133">Drei neue Methoden der Erweiterung der obersten Ebene im Zusammenhang mit MVC-Szenarien auf `IServiceCollection` stehen zur Verfügung.</span><span class="sxs-lookup"><span data-stu-id="b181b-133">Three new top-level extension methods related to MVC scenarios on `IServiceCollection` are available.</span></span> <span data-ttu-id="b181b-134">Vorlagen verwenden diese neuen Methoden anstelle von `UseMvc`.</span><span class="sxs-lookup"><span data-stu-id="b181b-134">Templates use these new methods instead of `UseMvc`.</span></span> <span data-ttu-id="b181b-135">Allerdings `AddMvc` weiterhin verhält sich wie in früheren Versionen hat.</span><span class="sxs-lookup"><span data-stu-id="b181b-135">However, `AddMvc` continues to behave as it has in previous releases.</span></span>

<span data-ttu-id="b181b-136">Das folgende Beispiel fügt Unterstützung für Controller und API-Funktionen, jedoch keine Sichten oder Seiten.</span><span class="sxs-lookup"><span data-stu-id="b181b-136">The following example adds support for controllers and API-related features, but not views or pages.</span></span> <span data-ttu-id="b181b-137">Dieser Code wird die API-Vorlage verwendet:</span><span class="sxs-lookup"><span data-stu-id="b181b-137">The API template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
}
```

<span data-ttu-id="b181b-138">Das folgende Beispiel fügt Unterstützung für Controller, API-Funktionen und Ansichten, aber nicht in Seiten.</span><span class="sxs-lookup"><span data-stu-id="b181b-138">The following example adds support for controllers, API-related features, and views, but not pages.</span></span> <span data-ttu-id="b181b-139">Dieser Code wird die Webanwendung (MVC)-Vorlage verwendet:</span><span class="sxs-lookup"><span data-stu-id="b181b-139">The Web Application (MVC) template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllersWithViews();
}
```

<span data-ttu-id="b181b-140">Das folgende Beispiel fügt Unterstützung für Razor-Seiten und minimale Controller.</span><span class="sxs-lookup"><span data-stu-id="b181b-140">The following example adds support for Razor Pages and minimal controller support.</span></span> <span data-ttu-id="b181b-141">Die Vorlage "Webanwendung" verwendet dieser Code:</span><span class="sxs-lookup"><span data-stu-id="b181b-141">The Web Application template uses this code:</span></span>

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddRazorPages();
}
```

<span data-ttu-id="b181b-142">Die neuen Methoden können auch kombiniert werden.</span><span class="sxs-lookup"><span data-stu-id="b181b-142">The new methods can also be combined.</span></span> <span data-ttu-id="b181b-143">Das folgende Beispiel entspricht dem Aufruf `AddMvc` in ASP.NET Core 2.2:</span><span class="sxs-lookup"><span data-stu-id="b181b-143">The following example is equivalent to calling `AddMvc` in ASP.NET Core 2.2:</span></span> 

```csharp
public void ConfigureServices(IServiceCollection services)
{
    services.AddControllers();
    services.AddRazorPages();
}
```

## <a name="update-routing-startup-code"></a><span data-ttu-id="b181b-144">Aktualisieren der routing-Startcode</span><span class="sxs-lookup"><span data-stu-id="b181b-144">Update routing startup code</span></span>

<span data-ttu-id="b181b-145">Wenn eine app aufruft `UseMvc` oder `UseSignalR`, migrieren Sie die app [Endpunkt Routing](xref:fundamentals/routing) Wenn möglich.</span><span class="sxs-lookup"><span data-stu-id="b181b-145">If an app calls `UseMvc` or `UseSignalR`, migrate the app to [Endpoint Routing](xref:fundamentals/routing) if possible.</span></span> <span data-ttu-id="b181b-146">Um die Kompatibilität mit früheren Versionen von MVC-Routing-Endpunkt zu verbessern, haben wir einige der Änderungen in URL-Generierung in ASP.NET Core 2.2 eingeführt wurden rückgängig gemacht.</span><span class="sxs-lookup"><span data-stu-id="b181b-146">To improve Endpoint Routing compatibility with previous versions of MVC, we've reverted some of the changes in URL generation introduced in ASP.NET Core 2.2.</span></span> <span data-ttu-id="b181b-147">Wenn Sie Probleme bei der Verwendung von Endpunkt-Routing in 2.2 aufgetreten ist, erwarten Sie Verbesserungen in ASP.NET Core 3.0, mit den folgenden Ausnahmen:</span><span class="sxs-lookup"><span data-stu-id="b181b-147">If you experienced problems using Endpoint Routing in 2.2, expect improvements in ASP.NET Core 3.0 with the following exceptions:</span></span>

* <span data-ttu-id="b181b-148">Wenn die app implementiert `IRouter` oder erbt von `Route`, empfiehlt es sich um zu vermeiden, Migrieren zu diesem Zeitpunkt.</span><span class="sxs-lookup"><span data-stu-id="b181b-148">If the app implements `IRouter` or inherits from `Route`, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="b181b-149">Ihr Feedback an [Planen der Migration von Implementierungen der IRouter basierend auf den Endpunkt Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span><span class="sxs-lookup"><span data-stu-id="b181b-149">Provide feedback at [Plan to migrate IRouter based implementations onto Endpoint Routing](https://github.com/aspnet/AspNetCore/issues/4221).</span></span>

* <span data-ttu-id="b181b-150">Wenn die app greift direkt auf `RouteData.Routers` in MVC, Sie möchten zu vermeiden, Migrieren zu diesem Zeitpunkt.</span><span class="sxs-lookup"><span data-stu-id="b181b-150">If the app directly accesses `RouteData.Routers` inside MVC, you may want to avoid migrating at this time.</span></span> <span data-ttu-id="b181b-151">Ihr Feedback an [migrationsanleitung für die Verwendung von RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span><span class="sxs-lookup"><span data-stu-id="b181b-151">Provide feedback at [Migration guidance for using RouteData.Routers](https://github.com/aspnet/AspNetCore/issues/9148).</span></span>

<span data-ttu-id="b181b-152">Endpunkt Routing unterstützt, die dieselbe Route Mustersyntax und die Routenmuster, die Erstellung von Features wie `IRouter`.</span><span class="sxs-lookup"><span data-stu-id="b181b-152">Endpoint Routing supports the same route pattern syntax and route pattern authoring features as `IRouter`.</span></span> <span data-ttu-id="b181b-153">Endpunkt Routing unterstützt `IRouteContraint`.</span><span class="sxs-lookup"><span data-stu-id="b181b-153">Endpoint Routing supports `IRouteContraint`.</span></span> <span data-ttu-id="b181b-154">Attributrouting unterstützt das Endpunkt `[Route]`, `[HttpGet]`, und die anderen MVC-routing-Attribute.</span><span class="sxs-lookup"><span data-stu-id="b181b-154">Endpoint routing supports `[Route]`, `[HttpGet]`, and the other MVC routing attributes.</span></span>

<span data-ttu-id="b181b-155">Für die meisten Anwendungen nur `Startup` sind Änderungen erforderlich.</span><span class="sxs-lookup"><span data-stu-id="b181b-155">For most applications, only `Startup` requires changes.</span></span>

### <a name="migrate-startupconfigure"></a><span data-ttu-id="b181b-156">Migrieren von Startup.Configure</span><span class="sxs-lookup"><span data-stu-id="b181b-156">Migrate Startup.Configure</span></span>

<span data-ttu-id="b181b-157">Ein allgemeiner Ratschlag:</span><span class="sxs-lookup"><span data-stu-id="b181b-157">General advice:</span></span>

* <span data-ttu-id="b181b-158">Hinzufügen `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="b181b-158">Add `UseRouting`.</span></span> 
* <span data-ttu-id="b181b-159">Wenn die app ruft `UseStaticFiles`, platzieren Sie `UseStaticFiles` **vor** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="b181b-159">If the app calls `UseStaticFiles`, place `UseStaticFiles` **before** `UseRouting`.</span></span>
* <span data-ttu-id="b181b-160">Wenn die app-Authentifizierung/-Autorisierung Funktionen, z. B. verwendet `AuthorizePage` oder `[Authorize]`, platzieren Sie den Aufruf von `UseAuthentication` und `UseAuthorization` **nach** `UseRouting`.</span><span class="sxs-lookup"><span data-stu-id="b181b-160">If the app uses authentication/authorization features such as `AuthorizePage` or `[Authorize]`, place the call to `UseAuthentication` and `UseAuthorization` **after** `UseRouting`.</span></span>
* <span data-ttu-id="b181b-161">Wenn die app verwendet [CORS](xref:security/cors) Funktionen, z. B. `[EnableCors]`, platzieren Sie `UseCors` weiter.</span><span class="sxs-lookup"><span data-stu-id="b181b-161">If the app uses [CORS](xref:security/cors) features, such as `[EnableCors]`, place `UseCors` next.</span></span>
* <span data-ttu-id="b181b-162">Ersetzen Sie dies `UseMvc` oder `UseSignalR` mit `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="b181b-162">Replace `UseMvc` or `UseSignalR` with `UseEndpoints`.</span></span>

<span data-ttu-id="b181b-163">Folgendes ist ein Beispiel für `Startup.Configure` in einer typischen ASP.NET Core-2.2-app:</span><span class="sxs-lookup"><span data-stu-id="b181b-163">The following is an example of `Startup.Configure` in a typical ASP.NET Core 2.2 app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseAuthentication();

    app.UseSignalR(hubs =>
    {
        hubs.MapHub<ChatHub>("/chat");
    });

    app.UseMvc(routes =>
    {
        routes.MapRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

<span data-ttu-id="b181b-164">Nach der Aktualisierung der vorherigen `Startup.Configure` Code:</span><span class="sxs-lookup"><span data-stu-id="b181b-164">After updating the previous `Startup.Configure` code:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseStaticFiles();
    
    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();
    app.UseCors();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>("/chat");
        endpoints.MapControllerRoute("default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="security-middleware-guidance"></a><span data-ttu-id="b181b-165">Middleware-Sicherheitsleitfaden</span><span class="sxs-lookup"><span data-stu-id="b181b-165">Security middleware guidance</span></span>

<span data-ttu-id="b181b-166">Unterstützung für die Autorisierung und CORS wird vereinheitlicht, um die [Middleware](xref:fundamentals/middleware/index) Ansatz.</span><span class="sxs-lookup"><span data-stu-id="b181b-166">Support for authorization and CORS is unified around the [middleware](xref:fundamentals/middleware/index) approach.</span></span> <span data-ttu-id="b181b-167">Dies ermöglicht die Verwendung derselben Middleware und Funktionen, in diesen Szenarios.</span><span class="sxs-lookup"><span data-stu-id="b181b-167">This allows use of the same middleware and functionality across these scenarios.</span></span> <span data-ttu-id="b181b-168">Eine aktualisierte autorisierungsmiddleware wird in dieser Version bereitgestellt, und CORS-Middleware wird verbessert, damit sie die Attribute, die von MVC-Controller verwendet verstehen kann.</span><span class="sxs-lookup"><span data-stu-id="b181b-168">An updated authorization middleware is provided in this release, and CORS Middleware is enhanced so that it can understand the attributes used by MVC controllers.</span></span>

#### <a name="cors"></a><span data-ttu-id="b181b-169">CORS</span><span class="sxs-lookup"><span data-stu-id="b181b-169">CORS</span></span>

<span data-ttu-id="b181b-170">Zuvor konnte CORS konfigurieren schwierig.</span><span class="sxs-lookup"><span data-stu-id="b181b-170">Previously, CORS could be difficult to configure.</span></span> <span data-ttu-id="b181b-171">Middleware wurde angegeben, für die Verwendung in einigen Fällen, jedoch MVC-Filter für die Verwendung vorgesehen waren **ohne** die Middleware in anderen Anwendungsfälle.</span><span class="sxs-lookup"><span data-stu-id="b181b-171">Middleware was provided for use in some use cases, but MVC filters were intended to be used **without** the middleware in other use cases.</span></span> <span data-ttu-id="b181b-172">Mit ASP.NET Core 3.0 empfehlen wir, dass alle apps, die CORS erfordern die CORS-Middleware zusammen mit dem Endpunkt Routing verwenden.</span><span class="sxs-lookup"><span data-stu-id="b181b-172">With ASP.NET Core 3.0, we recommend that all apps that require CORS use the CORS Middleware in tandem with Endpoint Routing.</span></span> <span data-ttu-id="b181b-173">`UseCors` eine Standardrichtlinie bereitgestellt werden können und `[EnableCors]` und `[DisableCors]` Attribute können verwendet werden, um die Standardrichtlinie überschreiben, sofern erforderlich.</span><span class="sxs-lookup"><span data-stu-id="b181b-173">`UseCors` can be provided with a default policy, and `[EnableCors]` and `[DisableCors]` attributes can be used to override the default policy where required.</span></span> 

<span data-ttu-id="b181b-174">Im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="b181b-174">In the following example:</span></span>

* <span data-ttu-id="b181b-175">CORS aktiviert ist, für alle Endpunkte mit dem `default` mit dem Namen Gruppenrichtlinie.</span><span class="sxs-lookup"><span data-stu-id="b181b-175">CORS is enabled for all endpoints with the `default` named policy.</span></span>
* <span data-ttu-id="b181b-176">Die `MyController` -Klasse deaktiviert CORS mithilfe der `[DisableCors]` Attribut.</span><span class="sxs-lookup"><span data-stu-id="b181b-176">The `MyController` class disables CORS with the `[DisableCors]` attribute.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseCors("default"); 

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[DisableCors]
public class MyController : ControllerBase
{
    ...
}
```

#### <a name="authorization"></a><span data-ttu-id="b181b-177">Autorisierung</span><span class="sxs-lookup"><span data-stu-id="b181b-177">Authorization</span></span>

<span data-ttu-id="b181b-178">In früheren Versionen von ASP.NET Core-Autorisierung-Unterstützung wurde angegeben, über die `[Authorize]` Attribut.</span><span class="sxs-lookup"><span data-stu-id="b181b-178">In earlier versions of ASP.NET Core, authorization support was provided via the `[Authorize]` attribute.</span></span> <span data-ttu-id="b181b-179">Autorisierungsmiddleware war nicht verfügbar.</span><span class="sxs-lookup"><span data-stu-id="b181b-179">Authorization middleware wasn't available.</span></span> <span data-ttu-id="b181b-180">In ASP.NET Core 3.0 ist die autorisierungsmiddleware erforderlich.</span><span class="sxs-lookup"><span data-stu-id="b181b-180">In ASP.NET Core 3.0, authorization middleware is required.</span></span> <span data-ttu-id="b181b-181">Es wird empfohlen, platzieren die Autorisierungsmiddleware für ASP.NET Core (`UseAuthorization`) unmittelbar nach `UseAuthentication`.</span><span class="sxs-lookup"><span data-stu-id="b181b-181">We recommend placing the ASP.NET Core Authorization Middleware (`UseAuthorization`) immediately after `UseAuthentication`.</span></span> <span data-ttu-id="b181b-182">Die Autorisierungsmiddleware kann auch mit einer Standardrichtlinie konfiguriert werden, die überschrieben werden kann.</span><span class="sxs-lookup"><span data-stu-id="b181b-182">The Authorization Middleware can also be configured with a default policy, which can be overridden.</span></span>

<span data-ttu-id="b181b-183">In ASP.NET Core 3.0 oder höher `UseAuthorization` unter "lautet" `Startup.Configure`, und die folgenden `HomeController` erfordert eine signierte Benutzer:</span><span class="sxs-lookup"><span data-stu-id="b181b-183">In ASP.NET Core 3.0 or later, `UseAuthorization` is called in `Startup.Configure`, and the following `HomeController` requires a signed in user:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

public class HomeController : ControllerBase
{
    [Authorize]
    public IActionResult BuyWidgets()
    {
        ...
    }
}
```

<span data-ttu-id="b181b-184">Wenn die app verwendet eine `AuthorizeFilter` als globalen Filter in MVC, es wird empfohlen den Code zum Bereitstellen einer Richtlinie für die Umgestaltung der `UseAuthorization` Middleware.</span><span class="sxs-lookup"><span data-stu-id="b181b-184">If the app uses an `AuthorizeFilter` as a global filter in MVC, we recommend refactoring the code to provide a policy to the `UseAuthorization` middleware.</span></span>

<span data-ttu-id="b181b-185">Im folgenden Beispiel eine benutzerdefinierte Richtlinie auf alle angewendet werden angefordert, wenn `UseAuthorization` aufgerufen wird, und die `HomeController` ermöglicht den Zugriff, ohne dass der Benutzer das Anmelden bei der app:</span><span class="sxs-lookup"><span data-stu-id="b181b-185">In the following example, a custom policy to be applied to all requests when `UseAuthorization` is called, and the `HomeController` allows access without the user signing into the app:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization(new AuthorizationPolicyBuilder().Build()));

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapDefaultControllerRoute();
    });
}

[AllowAnonymous]
public class HomeController : ControllerBase
{
    ...
}
```

<span data-ttu-id="b181b-186">Von Middleware-Autorisierung funktioniert, ohne das Framework, dass bestimmte Kenntnisse der Autorisierung.</span><span class="sxs-lookup"><span data-stu-id="b181b-186">Authorization by middleware works without the framework having any specific knowledge of authorization.</span></span> <span data-ttu-id="b181b-187">Z. B. [integritätsprüfungen](xref:host-and-deploy/health-checks) enthält keine spezifischen Wissensquellen für die Autorisierung, aber integritätsprüfungen können eine konfigurierbare Autorisierungsrichtlinie, die von der Middleware angewendet.</span><span class="sxs-lookup"><span data-stu-id="b181b-187">For instance, [health checks](xref:host-and-deploy/health-checks) has no specific knowledge of authorization, but health checks can have a configurable authorization policy applied by the middleware.</span></span>

<span data-ttu-id="b181b-188">Im folgenden Beispiel `UseAuthorization` Autorisierung ohne eine Standardrichtlinie, verarbeitet aber die `/healthz` integritätsüberprüfung muss der Benutzer in der `admin` Rolle:</span><span class="sxs-lookup"><span data-stu-id="b181b-188">In the following example, `UseAuthorization` processes authorization without a default policy, but the `/healthz` health check endpoint requires the user to be in the `admin` role:</span></span> 

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseAuthentication();
    app.UseAuthorization();

    app.UseEndpoints(endpoints =>
    {
        endpoints
            .MapHealthChecks("/healthz")
            .RequireAuthorization(new AuthorizeAttribute(){ Roles = "admin", });
    });
}
```

<span data-ttu-id="b181b-189">Schutz wird für einige Szenarien implementiert werden.</span><span class="sxs-lookup"><span data-stu-id="b181b-189">Protection is implemented for some scenarios.</span></span> <span data-ttu-id="b181b-190">`UseEndpoint` Middleware wird eine Ausnahme ausgelöst, wenn eine Autorisierung oder CORS-Richtlinie aufgrund fehlender Middleware übersprungen wird.</span><span class="sxs-lookup"><span data-stu-id="b181b-190">`UseEndpoint` middleware throws an exception if an authorization or CORS policy is skipped due to missing middleware.</span></span> <span data-ttu-id="b181b-191">Unterstützung der Analyzer angeben, dass zusätzliches Feedback zu einer fehlerhaften Konfiguration ausgeführt wird.</span><span class="sxs-lookup"><span data-stu-id="b181b-191">Analyzer support to provide additional feedback about misconfiguration is in progress.</span></span>

### <a name="migrate-signalr"></a><span data-ttu-id="b181b-192">Migrieren von SignalR</span><span class="sxs-lookup"><span data-stu-id="b181b-192">Migrate SignalR</span></span>

<span data-ttu-id="b181b-193">Zuordnen von SignalR-Hubs jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="b181b-193">Mapping of SignalR hubs now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="b181b-194">Ordnen Sie jeden Hub mit `MapHub`.</span><span class="sxs-lookup"><span data-stu-id="b181b-194">Map each hub with `MapHub`.</span></span> <span data-ttu-id="b181b-195">Jeder Hub ist explizit aufgelistet, wie in früheren Versionen.</span><span class="sxs-lookup"><span data-stu-id="b181b-195">As in previous versions, each hub is explicitly listed.</span></span>

<span data-ttu-id="b181b-196">Im folgenden Beispiel, Unterstützung für die `ChatHub` SignalR-Hub hinzugefügt wird:</span><span class="sxs-lookup"><span data-stu-id="b181b-196">In the following example, support for the `ChatHub` SignalR hub is added:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHub<ChatHub>();
    });
}
```

### <a name="migrate-mvc-controllers"></a><span data-ttu-id="b181b-197">Migrieren von MVC-Controller</span><span class="sxs-lookup"><span data-stu-id="b181b-197">Migrate MVC controllers</span></span>

<span data-ttu-id="b181b-198">Zuordnung der Controller jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="b181b-198">Mapping of controllers now takes place inside `UseEndpoints`.</span></span> 

<span data-ttu-id="b181b-199">Hinzufügen `MapControllers` Wenn die app das attributrouting verwendet.</span><span class="sxs-lookup"><span data-stu-id="b181b-199">Add `MapControllers` if the app uses attribute routing.</span></span> <span data-ttu-id="b181b-200">Da die routing-Unterstützung für viele Frameworks in ASP.NET Core 3.0 oder höher enthält, ist das Hinzufügen von Attribut-Routing-Controller teilnehmen.</span><span class="sxs-lookup"><span data-stu-id="b181b-200">Since routing includes support for many frameworks in ASP.NET Core 3.0 or later, adding attribute-routed controllers is opt-in.</span></span> 

<span data-ttu-id="b181b-201">Ersetzen Sie die folgenden:</span><span class="sxs-lookup"><span data-stu-id="b181b-201">Replace the following:</span></span>

* <span data-ttu-id="b181b-202">`MapRoute` mit `MapControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="b181b-202">`MapRoute` with `MapControllerRoute`</span></span>
* <span data-ttu-id="b181b-203">`MapAreaRoute` mit `MapAreaControllerRoute`</span><span class="sxs-lookup"><span data-stu-id="b181b-203">`MapAreaRoute` with `MapAreaControllerRoute`</span></span>

<span data-ttu-id="b181b-204">Da routing jetzt Unterstützung für mehr als nur MVC enthält, wurde die Terminologie geändert, um stellen diese Methoden, die eindeutig angeben, was sie tun.</span><span class="sxs-lookup"><span data-stu-id="b181b-204">Since routing now includes support for more than just MVC, the terminology has changed to make these methods clearly state what they do.</span></span> <span data-ttu-id="b181b-205">Herkömmliche Routen wie z. B. `MapControllerRoute` / `MapAreaControllerRoute` / `MapDefaultControllerRoute` gelten in der Reihenfolge, die sie hinzugefügt werden.</span><span class="sxs-lookup"><span data-stu-id="b181b-205">Conventional routes such as `MapControllerRoute`/`MapAreaControllerRoute`/`MapDefaultControllerRoute` are applied in the order that they're added.</span></span> <span data-ttu-id="b181b-206">Platzieren Sie zuerst die spezifischere Routen (z. B. die Routen für einen Bereich).</span><span class="sxs-lookup"><span data-stu-id="b181b-206">Place more specific routes (such as routes for an area) first.</span></span>

<span data-ttu-id="b181b-207">Im folgenden Beispiel:</span><span class="sxs-lookup"><span data-stu-id="b181b-207">In the following example:</span></span>

* <span data-ttu-id="b181b-208">`MapControllers` Fügt Unterstützung für die Attribut-Routing-Controller hinzu.</span><span class="sxs-lookup"><span data-stu-id="b181b-208">`MapControllers` adds support for attribute-routed controllers.</span></span>
* <span data-ttu-id="b181b-209">`MapAreaControllerRoute` Fügt eine konventionelle Route für Controller in einem Bereich hinzu.</span><span class="sxs-lookup"><span data-stu-id="b181b-209">`MapAreaControllerRoute` adds a conventional route for controllers in an area.</span></span>
* <span data-ttu-id="b181b-210">`MapControllerRoute` Fügt eine konventionelle Route für Controller.</span><span class="sxs-lookup"><span data-stu-id="b181b-210">`MapControllerRoute` adds a conventional route for controllers.</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapControllers();
        endpoints.MapAreaControllerRoute(
            "admin", 
            "admin", 
            "Admin/{controller=Home}/{action=Index}/{id?}");
        endpoints.MapControllerRoute(
            "default", "{controller=Home}/{action=Index}/{id?}");
    });
}
```

### <a name="migrate-razor-pages"></a><span data-ttu-id="b181b-211">Migrieren von Razor-Seiten</span><span class="sxs-lookup"><span data-stu-id="b181b-211">Migrate Razor Pages</span></span>

<span data-ttu-id="b181b-212">Zuordnen von Razor-Seiten jetzt findet in `UseEndpoints`.</span><span class="sxs-lookup"><span data-stu-id="b181b-212">Mapping Razor Pages now takes place inside `UseEndpoints`.</span></span>

<span data-ttu-id="b181b-213">Hinzufügen `MapRazorPages` Wenn die app mit Razor-Seiten verwendet.</span><span class="sxs-lookup"><span data-stu-id="b181b-213">Add `MapRazorPages` if the app uses Razor Pages.</span></span> <span data-ttu-id="b181b-214">Da der Endpunkt-Routing-Unterstützung für viele Frameworks enthält, ist das Hinzufügen von Razor-Seiten jetzt teilnehmen.</span><span class="sxs-lookup"><span data-stu-id="b181b-214">Since Endpoint Routing includes support for many frameworks, adding Razor Pages is now opt-in.</span></span>

<span data-ttu-id="b181b-215">Im folgenden Beispiel `MapRazorPages` fügt Unterstützung für Razor-Seiten hinzu:</span><span class="sxs-lookup"><span data-stu-id="b181b-215">In the following example, `MapRazorPages` adds support for Razor Pages:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapRazorPages();
    });
}
```

### <a name="use-mvc-without-endpoint-routing"></a><span data-ttu-id="b181b-216">Verwenden Sie MVC ohne Endpunkt weiterzuleiten.</span><span class="sxs-lookup"><span data-stu-id="b181b-216">Use MVC without Endpoint Routing</span></span>

<span data-ttu-id="b181b-217">Mit MVC über `UseMvc` oder `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 erfordert eine explizite opt-in in `Startup.ConfigureServices`.</span><span class="sxs-lookup"><span data-stu-id="b181b-217">Using MVC via `UseMvc` or `UseMvcWithDefaultRoute` in ASP.NET Core 3.0 requires an explicit opt-in inside `Startup.ConfigureServices`.</span></span> <span data-ttu-id="b181b-218">Dies ist erforderlich, da MVC wissen muss, ob sie auf die Autorisierungs- und CORS-Middleware, während der Initialisierung verlassen kann.</span><span class="sxs-lookup"><span data-stu-id="b181b-218">This is required because MVC must know whether it can rely on the authorization and CORS Middleware during initialization.</span></span> <span data-ttu-id="b181b-219">Ein Analyzer wird bereitgestellt, die warnt, wenn die app versucht, eine nicht unterstützte Konfiguration zu verwenden.</span><span class="sxs-lookup"><span data-stu-id="b181b-219">An analyzer is provided that warns if the app attempts to use an unsupported configuration.</span></span>

<span data-ttu-id="b181b-220">Wenn die app ältere "requires" `IRouter` unterstützen, deaktivieren Sie `EnableEndpointRouting` mithilfe einer der folgenden Ansätze `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="b181b-220">If the app requires legacy `IRouter` support, disable `EnableEndpointRouting` using any of the following approaches in `Startup.ConfigureServices`:</span></span>

```csharp
services.AddMvc(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllers(options => options.EnableEndpointRouting = false);
```

```csharp
services.AddControllersWithViews(options => options.EnableEndpointRouting = false);

```

```csharp
services.AddRazorPages().AddMvcOptions(options => options.EnableEndpointRouting = false);
```

### <a name="migrate-health-checks"></a><span data-ttu-id="b181b-221">Migrieren von integritätsprüfungen</span><span class="sxs-lookup"><span data-stu-id="b181b-221">Migrate health checks</span></span>

<span data-ttu-id="b181b-222">Integritätsprüfungen können verwendet werden, als eine *Router Adware* mit Routing-Endpunkt.</span><span class="sxs-lookup"><span data-stu-id="b181b-222">Health checks can be used as a *router-ware* with Endpoint Routing.</span></span>

<span data-ttu-id="b181b-223">Hinzufügen `MapHealthChecks` Routing-Endpunkt mit der Integrität überprüft.</span><span class="sxs-lookup"><span data-stu-id="b181b-223">Add `MapHealthChecks` to use health checks with Endpoint Routing.</span></span> <span data-ttu-id="b181b-224">Die `MapHealthChecks` Methode akzeptiert Argumente, die ähnlich wie `UseHealthChecks`.</span><span class="sxs-lookup"><span data-stu-id="b181b-224">The `MapHealthChecks` method accepts arguments similar to `UseHealthChecks`.</span></span> <span data-ttu-id="b181b-225">Der Vorteil der Verwendung `MapHealthChecks` über `UseHealthChecks` ist die Fähigkeit, Autorisierung anzuwenden und höher eine präzisere Kontrolle über die entsprechende Richtlinie haben.</span><span class="sxs-lookup"><span data-stu-id="b181b-225">The advantage of using `MapHealthChecks` over `UseHealthChecks` is the ability to apply authorization and to have greater fine-grained control over the matching policy.</span></span> 

<span data-ttu-id="b181b-226">Im folgenden Beispiel `MapHealthChecks` wird aufgerufen, für einen Endpunkt auf Kontrollkästchen `/healthz`:</span><span class="sxs-lookup"><span data-stu-id="b181b-226">In the following example, `MapHealthChecks` is called for a health check endpoint at `/healthz`:</span></span>

```csharp
public void Configure(IApplicationBuilder app)
{
    ...

    app.UseRouting();

    app.UseEndpoints(endpoints =>
    {
        endpoints.MapHealthChecks("/healthz", new HealthCheckOptions() { });
    });
}
```

## <a name="hostbuilder-replaces-webhostbuilder"></a><span data-ttu-id="b181b-227">HostBuilder ersetzt WebHostBuilder</span><span class="sxs-lookup"><span data-stu-id="b181b-227">HostBuilder replaces WebHostBuilder</span></span>

<span data-ttu-id="b181b-228">Verwenden Sie die Vorlagen für ASP.NET Core 3.0 [generischer Host](xref:fundamentals/host/generic-host).</span><span class="sxs-lookup"><span data-stu-id="b181b-228">The ASP.NET Core 3.0 templates use [Generic Host](xref:fundamentals/host/generic-host).</span></span> <span data-ttu-id="b181b-229">Frühere Versionen verwendeten [Webhost](xref:fundamentals/host/web-host).</span><span class="sxs-lookup"><span data-stu-id="b181b-229">Previous versions used [Web Host](xref:fundamentals/host/web-host).</span></span> <span data-ttu-id="b181b-230">Der folgende Code zeigt die generierte Vorlage für ASP.NET Core 3.0 `Program` Klasse:</span><span class="sxs-lookup"><span data-stu-id="b181b-230">The following code shows the ASP.NET Core 3.0 template generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program.cs?name=snippet)]

<span data-ttu-id="b181b-231">Der folgende Code zeigt die ASP.NET Core-2.2 Vorlage generierte `Program` Klasse:</span><span class="sxs-lookup"><span data-stu-id="b181b-231">The following code shows the ASP.NET Core 2.2 template-generated `Program` class:</span></span>

[!code-csharp[](22-to-30/samples/Program2.2.cs?name=snippet)]

<span data-ttu-id="b181b-232"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> verbleibt im 3.0, und der Typ des der `webBuilder` im obigen Codebeispiel angezeigt.</span><span class="sxs-lookup"><span data-stu-id="b181b-232"><xref:Microsoft.AspNetCore.Hosting.IWebHostBuilder> remains in 3.0 and is the type of the `webBuilder` seen in the preceding code sample.</span></span> <span data-ttu-id="b181b-233"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> wird in einer zukünftigen Version als veraltet markiert und ersetzt Sie durch `HostBuilder`.</span><span class="sxs-lookup"><span data-stu-id="b181b-233"><xref:Microsoft.AspNetCore.Hosting.WebHostBuilder> will be deprecated in a future release and replaced by `HostBuilder`.</span></span>

<span data-ttu-id="b181b-234">Die wichtigste Neuerung von `WebHostBuilder` zu `HostBuilder` befindet sich im [Abhängigkeitsinjektion (Dependency Injection)](xref:fundamentals/dependency-injection).</span><span class="sxs-lookup"><span data-stu-id="b181b-234">The most significant change from `WebHostBuilder` to `HostBuilder` is in [dependency injection (DI)](xref:fundamentals/dependency-injection).</span></span> <span data-ttu-id="b181b-235">Bei Verwendung `HostBuilder`, Sie können nur Folgendes einfügen: <xref:Microsoft.Extensions.Configuration.IConfiguration> und <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> in `Startup`Konstruktor.</span><span class="sxs-lookup"><span data-stu-id="b181b-235">When using `HostBuilder`, you can only inject <xref:Microsoft.Extensions.Configuration.IConfiguration> and <xref:Microsoft.AspNetCore.Hosting.IHostingEnvironment> into `Startup`'s constructor.</span></span> <span data-ttu-id="b181b-236">Die `HostBuilder` DI Einschränkungen:</span><span class="sxs-lookup"><span data-stu-id="b181b-236">The `HostBuilder` DI constraints:</span></span>

* <span data-ttu-id="b181b-237">Aktivieren Sie den DI-Container nur ein Mal erstellt werden sollen.</span><span class="sxs-lookup"><span data-stu-id="b181b-237">Enable the DI container to be built only one time.</span></span>
* <span data-ttu-id="b181b-238">Vermeiden die resultierenden Probleme mit der Objektlebensdauer wie das Auflösen von mehrere Instanzen des Singletons.</span><span class="sxs-lookup"><span data-stu-id="b181b-238">Avoids the resulting object lifetime issues like resolving multiple instances of singletons.</span></span>

## <a name="update-signalr-code"></a><span data-ttu-id="b181b-239">Aktualisieren Sie Code, SignalR</span><span class="sxs-lookup"><span data-stu-id="b181b-239">Update SignalR code</span></span>

<span data-ttu-id="b181b-240">`System.Text.Json` ist nun standardmäßig vom Client und Server, der Hub-Protokoll verwendet.</span><span class="sxs-lookup"><span data-stu-id="b181b-240">`System.Text.Json` is now the default Hub Protocol used by both the client and server.</span></span>

<span data-ttu-id="b181b-241">In `Startup.ConfigureServices`, rufen Sie `AddJsonProtocol` serialisiereroptionen festlegen.</span><span class="sxs-lookup"><span data-stu-id="b181b-241">In `Startup.ConfigureServices`, call `AddJsonProtocol` to set serializer options.</span></span>

<span data-ttu-id="b181b-242">**Server:**</span><span class="sxs-lookup"><span data-stu-id="b181b-242">**Server:**</span></span>

```csharp
services.AddSignalR(...)
        .AddJsonProtocol(options =>
        {
            options.WriteIndented = false;
        })
```

<span data-ttu-id="b181b-243">**Client:**</span><span class="sxs-lookup"><span data-stu-id="b181b-243">**Client:**</span></span>

```csharp
new HubConnectionBuilder()
    .WithUrl("/chatHub")
    .AddJsonProtocol(options =>
    {
        options.WriteIndented = false;
    })
    .Build();
```

### <a name="switch-to-newtonsoftjson"></a><span data-ttu-id="b181b-244">Wechseln Sie zu "newtonsoft.JSON"</span><span class="sxs-lookup"><span data-stu-id="b181b-244">Switch to Newtonsoft.Json</span></span>

<span data-ttu-id="b181b-245">Bei Verwendung von Features von `Newtonsoft.Json` , die nicht unterstützt `System.Text.Json`, können Sie zum wechseln `Newtonsoft.Json`:</span><span class="sxs-lookup"><span data-stu-id="b181b-245">If you're using features of `Newtonsoft.Json` that aren't supported in `System.Text.Json`, you can switch back to `Newtonsoft.Json`:</span></span>

1. <span data-ttu-id="b181b-246">Installieren Sie die [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet-Paket.</span><span class="sxs-lookup"><span data-stu-id="b181b-246">Install the [Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson](https://www.nuget.org/packages/Microsoft.AspNetCore.SignalR.Protocols.NewtonsoftJson) NuGet package.</span></span>
1. <span data-ttu-id="b181b-247">Verketten von auf dem Client eine `AddNewtonsoftJsonProtocol` Methodenaufruf der `HubConnectionBuilder` Instanz:</span><span class="sxs-lookup"><span data-stu-id="b181b-247">On the client, chain an `AddNewtonsoftJsonProtocol` method call to the `HubConnectionBuilder` instance:</span></span>

    ```csharp
    new HubConnectionBuilder()
        .WithUrl("/chatHub")
        .AddNewtonsoftJsonProtocol(...)
        .Build();
    ```

1. <span data-ttu-id="b181b-248">Verketten von auf dem Server ein `AddNewtonsoftJsonProtocol` Methodenaufruf der `AddSignalR` -Methodenaufruf in `Startup.ConfigureServices`:</span><span class="sxs-lookup"><span data-stu-id="b181b-248">On the server, chain an `AddNewtonsoftJsonProtocol` method call to the `AddSignalR` method call in `Startup.ConfigureServices`:</span></span>

    ```csharp
    services.AddSignalR()
        .AddNewtonsoftJsonProtocol(...);
    ```

## <a name="opt-in-to-runtime-compilation"></a><span data-ttu-id="b181b-249">Abonnieren Sie Runtime-Kompilierung</span><span class="sxs-lookup"><span data-stu-id="b181b-249">Opt in to runtime compilation</span></span>

<span data-ttu-id="b181b-250">In 3.0 ist die Runtime-Kompilierung ein opt-in-Szenario.</span><span class="sxs-lookup"><span data-stu-id="b181b-250">In 3.0, runtime compilation is an opt-in scenario.</span></span> <span data-ttu-id="b181b-251">Um die Runtime-Kompilierung zu aktivieren, finden Sie unter <xref:mvc/views/view-compilation#runtime-compilation>.</span><span class="sxs-lookup"><span data-stu-id="b181b-251">To enable runtime compilation, see <xref:mvc/views/view-compilation#runtime-compilation>.</span></span>
